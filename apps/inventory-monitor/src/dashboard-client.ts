export const DASHBOARD_CLIENT_CSS = `
.shop-picker{display:flex!important;align-items:center!important;gap:6px!important;color:var(--muted)!important;font-size:10px!important}.shop-picker select{height:31px!important;min-width:170px!important;border:1px solid #d0d5dd!important;border-radius:4px!important;background:#fff!important;color:#344054!important;padding:0 28px 0 8px!important;font:600 10px inherit!important}.metric:before{display:none!important}
.hero{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:24px;align-items:end;padding:16px 0}.hero h1{margin:0;color:#171717;font-size:30px;letter-spacing:-.035em}.hero p{margin:7px 0 0;color:#737373;font-size:12px}.hero-alert{display:flex;align-items:center;gap:10px;min-width:420px;padding:12px 15px;border:1px solid #d9e7df;border-radius:6px;background:#f7fcf9;color:var(--green);font-size:11px}.hero-alert i{width:8px;height:8px;border-radius:50%;background:currentColor}.hero-alert strong{font-size:13px}.hero-alert span{color:#777}.hero-alert.critical{border-color:#ecc9c9;background:#fff8f8;color:var(--red)}.hero-alert.warning{border-color:#ead7b9;background:#fffaf2;color:var(--amber)}.hero-alert.unknown{border-color:#dedede;background:#fafafa;color:#777}.brand-mark{display:grid!important;width:34px!important;height:34px!important;margin-right:12px!important;border-radius:9px!important;background:#171717!important;color:#fff!important;place-items:center!important;font-size:17px!important;font-weight:750!important}.brand p{display:block!important;margin:0 0 2px!important;color:#8a8a8a!important;font-size:10px!important;letter-spacing:0!important}.brand h1{font-size:15px!important}.severity-pill.critical{background:#fff0ef!important;color:#c93636!important}.severity-pill.warning{background:#fff7e9!important;color:#986013!important}.severity-pill.unknown{background:#f1f1f1!important;color:#666!important}
.shop-label{display:inline-flex;align-items:center;padding:3px 6px;border-radius:3px;background:#f2f4f7;color:#475467;font-size:9px;font-weight:600}.shop-status-list{display:grid;gap:0}.shop-status{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:8px;padding:9px 0;border-bottom:1px solid #eaecf0}.shop-status:last-child{border-bottom:0}.shop-status strong{display:block;font-size:10px}.shop-status p{margin:3px 0 0;color:var(--muted);font-size:9px}.shop-status .shop-numbers{display:flex;gap:4px;align-items:center}.queue-note{padding:12px;border:1px solid #e1e4e8;border-radius:4px;background:#fafafa;color:#667085;font-size:10px;margin-bottom:10px}.inventory-table .shop-column{width:150px}
:root{--navy:#172033;--blue:#245b91;--green:#147a55;--red:#b42318;--amber:#a15c07;--line:#dfe3e8;--muted:#667085;font-family:Inter,"PingFang SC","Microsoft YaHei",sans-serif;color:var(--navy);background:#f5f6f7;font-synthesis:none}*{box-sizing:border-box}body{margin:0;background:#f5f6f7;min-width:320px}header{height:64px;padding:0 24px;background:#fff;border-bottom:1px solid var(--line);display:flex;align-items:center;justify-content:space-between;position:sticky;top:0;z-index:20}.brand{display:flex;align-items:center}.brand-mark,.brand p,.eyebrow{display:none}.brand h1{margin:0;font-size:18px;font-weight:650;letter-spacing:0;color:#101828}.header-meta{display:flex;gap:10px;align-items:center;margin-top:4px;color:var(--muted);font-size:11px}.header-meta code{font-family:"SFMono-Regular",Consolas,monospace;color:#344054}.header-actions{display:flex;align-items:center;gap:8px}.status-chip{display:inline-flex;align-items:center;gap:6px;padding:5px 8px;border:1px solid #b7dfcc;border-radius:4px;background:#f6fef9;color:var(--green);font-size:11px}.status-chip i{width:6px;height:6px;border-radius:50%;background:currentColor}.status-chip.warning{border-color:#f0d7ad;background:#fffbeb;color:var(--amber)}.status-chip.critical{border-color:#f2c7c4;background:#fff7f6;color:var(--red)}button{border:1px solid #344054;border-radius:4px;background:#344054;color:#fff;padding:7px 11px;font:600 11px inherit;cursor:pointer}button:hover{background:#1d2939}button.secondary{border-color:#d0d5dd;background:#fff;color:#344054}button.secondary:hover{background:#f9fafb}button:disabled{opacity:.55;cursor:not-allowed}main{max-width:1600px;margin:0 auto;padding:18px 22px 50px}.metrics{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));background:#fff;border:1px solid var(--line);border-radius:5px}.metric{padding:14px 16px;border-right:1px solid var(--line);min-height:82px}.metric:last-child{border-right:0}.metric small{display:block;color:var(--muted);font-size:11px}.metric strong{display:block;margin-top:8px;font-size:22px;font-weight:650;color:#101828}.metric span{display:block;margin-top:4px;color:#7b8494;font-size:10px}.metric.critical strong{color:var(--red)}.metric.warning strong{color:var(--amber)}.metric.good strong{color:var(--green)}.priority-grid{display:grid;grid-template-columns:minmax(0,1.55fr) minmax(320px,.65fr);gap:12px;margin-top:12px}.panel,.product{background:#fff;border:1px solid var(--line);border-radius:5px;box-shadow:none}.panel{padding:16px}.section-title{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:12px}.section-title h2,.panel>h2{margin:0;font-size:14px;font-weight:650;color:#101828}.count-badge{min-width:24px;height:24px;display:grid;place-items:center;border:1px solid var(--line);border-radius:4px;background:#f9fafb;font-size:11px;font-weight:650}.incidents-panel{min-height:260px}.quality-summary{display:grid;grid-template-columns:8px minmax(0,1fr) auto;gap:10px;align-items:start;padding:12px;border:1px solid #f0d7ad;border-radius:4px;background:#fffbeb;margin-bottom:10px}.quality-summary i{width:8px;height:8px;border-radius:50%;background:var(--amber);margin-top:4px}.quality-summary strong{display:block;font-size:11px;color:#7a2e0e}.quality-summary p{margin:4px 0 0;color:#854d0e;font-size:10px;line-height:1.5}.quality-summary span{font-size:10px;font-weight:650;color:var(--amber);white-space:nowrap}.risk-table{width:100%;border-collapse:collapse}.risk-table th,.risk-table td{text-align:left;padding:9px 8px;border-bottom:1px solid #eaecf0;font-size:11px;vertical-align:top}.risk-table th{background:#f9fafb;color:var(--muted);font-weight:600}.risk-table tr:last-child td{border-bottom:0}.severity-pill{display:inline-flex;padding:3px 6px;border-radius:3px;background:#f2f4f7;font-size:10px;font-weight:650}.severity-pill.critical{background:#fef3f2;color:var(--red)}.severity-pill.warning{background:#fffaeb;color:var(--amber)}.severity-pill.unknown{color:#475467}.risk-title{font-weight:600;color:#101828}.risk-reason{max-width:360px;color:#475467;line-height:1.45}.id-list{display:grid;gap:3px}.id-list code,.id-line code{font-family:"SFMono-Regular",Consolas,monospace;font-size:10px;color:#344054}.inline-copy{border:0;background:transparent;color:var(--blue);padding:0 0 0 5px;font-size:9px}.inline-copy:hover{background:transparent;text-decoration:underline}.reminder{display:grid;grid-template-columns:8px 1fr;gap:9px;padding:10px 0;border-bottom:1px solid #eaecf0}.reminder:last-child{border-bottom:0}.reminder-indicator{width:7px;height:7px;border-radius:50%;margin-top:4px;background:#98a2b3}.reminder.critical .reminder-indicator{background:var(--red)}.reminder.warning .reminder-indicator{background:var(--amber)}.reminder h3{margin:0 0 3px;font-size:11px;font-weight:650}.reminder p{margin:0;color:var(--muted);font-size:10px;line-height:1.5}.reminder-action{display:block;margin-top:3px;color:var(--blue)}.empty{padding:28px 12px;text-align:center;color:var(--muted);font-size:11px}.empty strong{display:block;margin-bottom:4px;color:#344054;font-size:12px}.readiness-panel,.backtest-panel,.inventory-panel,.details-grid{margin-top:12px}.readiness-panel #readiness{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));border-top:1px solid #eaecf0;border-left:1px solid #eaecf0}.readiness-item{padding:10px 12px;border-right:1px solid #eaecf0;border-bottom:1px solid #eaecf0}.readiness-item span{display:block;color:var(--muted);font-size:10px}.readiness-item strong{display:block;margin-top:5px;font-size:11px;font-weight:600}.readiness-item.wide{grid-column:span 2}.readiness-item .bar{height:5px;margin-top:7px;background:#eaecf0;display:flex}.readiness-item .bar i:nth-child(1){background:var(--green)}.readiness-item .bar i:nth-child(2){background:#d99a31}.readiness-item .bar i:nth-child(3){background:#98a2b3}.bar-legend{display:flex;gap:8px;flex-wrap:wrap;margin-top:5px;color:var(--muted);font-size:9px}.live-dot{display:none}.inventory-panel{padding:0}.inventory-panel .section-title{padding:14px 16px;margin:0;border-bottom:1px solid var(--line)}.inventory-heading-meta{margin-top:4px;color:var(--muted);font-size:10px}.table-tools{display:flex;align-items:center;gap:7px}.table-tools input,.table-tools select{height:31px;border:1px solid #d0d5dd;border-radius:4px;background:#fff;color:#344054;padding:0 9px;font:11px inherit}.table-tools input{width:260px}.table-tools input:focus,.table-tools select:focus{outline:2px solid #cfe0f2;outline-offset:0;border-color:#7fa6cc}.inventory-scroll{overflow:auto;max-height:720px}.inventory-table{width:100%;border-collapse:separate;border-spacing:0}.inventory-table>thead{position:sticky;top:0;z-index:3}.inventory-table th,.inventory-table td{text-align:left;padding:9px 12px;border-bottom:1px solid #eaecf0;font-size:10px;vertical-align:middle}.inventory-table th{background:#f9fafb;color:#667085;font-weight:600;white-space:nowrap}.inventory-table .product-summary:hover{background:#fafbfc}.product-name{font-size:11px;font-weight:600;color:#101828}.product-sub{display:flex;gap:8px;margin-top:3px;color:var(--muted);font-size:9px}.product-sub code{font-family:"SFMono-Regular",Consolas,monospace}.mapping{font-weight:600}.mapping.high{color:var(--green)}.mapping.medium{color:var(--amber)}.product-detail td{padding:0 20px 14px;background:#fafbfc}.sku-table{width:100%;border-collapse:collapse;border:1px solid #eaecf0;background:#fff}.sku-table th,.sku-table td{padding:7px 9px;border-bottom:1px solid #eaecf0;font-size:9px}.sku-table th{position:static;background:#f9fafb}.sku-table tr:last-child td{border-bottom:0}.id-line{display:flex;align-items:center;gap:4px;margin:2px 0}.channels{line-height:1.5}.data-unknown{color:#7b8494}.product-toggle{padding:4px 7px;background:#fff;color:#344054;border-color:#d0d5dd;font-size:9px}.details-grid{display:grid;grid-template-columns:minmax(280px,.65fr) minmax(0,1.35fr);gap:12px}.rule{display:grid;grid-template-columns:120px 1fr;gap:10px;padding:8px 0;border-bottom:1px solid #eaecf0;font-size:10px}.rule:last-child{border-bottom:0}.rule small{color:var(--muted)}.rule strong{font-weight:550}.schedule-table{width:100%;border-collapse:collapse}.schedule-table th,.schedule-table td{padding:8px;border-bottom:1px solid #eaecf0;text-align:left;font-size:10px;vertical-align:top}.schedule-table th{background:#f9fafb;color:var(--muted);font-weight:600}.chart-layout{display:grid;grid-template-columns:minmax(0,1fr) 170px;gap:16px;align-items:center}.chart-wrap{height:220px}.chart-wrap svg{width:100%;height:100%;display:block}.chart-metrics{border-left:1px solid #eaecf0}.chart-metric{display:flex;align-items:center;justify-content:space-between;padding:8px 0 8px 14px;border-bottom:1px solid #eaecf0}.chart-metric:last-child{border-bottom:0}.chart-metric small{color:var(--muted);font-size:9px}.chart-metric strong{font-size:11px}.sonner-region{position:fixed;right:18px;bottom:18px;z-index:1000;width:min(360px,calc(100vw - 24px));display:flex;flex-direction:column-reverse;gap:7px;pointer-events:none}.sonner-toast{pointer-events:auto;display:grid;grid-template-columns:22px minmax(0,1fr) auto;gap:9px;align-items:start;padding:10px;border:1px solid #d0d5dd;border-radius:5px;background:#fff;box-shadow:0 8px 24px #10182818;animation:sonner-in .16s ease-out}.sonner-toast.success{border-left:3px solid var(--green)}.sonner-toast.error{border-left:3px solid var(--red)}.sonner-toast.warning{border-left:3px solid var(--amber)}.sonner-icon{width:20px;height:20px;display:grid;place-items:center;color:#475467;font-size:11px;font-weight:700}.sonner-copy strong{display:block;margin:0 0 2px;font-size:11px}.sonner-copy p{margin:0;color:var(--muted);font-size:9px;line-height:1.4;word-break:break-word}.sonner-close{padding:0;border:0;background:transparent;color:#98a2b3;font-size:14px}.sonner-close:hover{background:transparent}.sonner-toast.leaving{animation:sonner-out .14s ease-in forwards}@keyframes sonner-in{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:none}}@keyframes sonner-out{to{opacity:0;transform:translateY(6px)}}
@media(max-width:1100px){.metrics{grid-template-columns:repeat(3,1fr)}.metric:nth-child(3){border-right:0}.metric:nth-child(n+4){border-top:1px solid var(--line)}.priority-grid,.details-grid{grid-template-columns:1fr}.readiness-panel #readiness{grid-template-columns:repeat(3,1fr)}}
@media(max-width:760px){header{height:auto;padding:12px;align-items:flex-start;gap:10px}.header-actions{flex-wrap:wrap;justify-content:flex-end}main{padding:12px}.hero{grid-template-columns:1fr}.hero-alert{min-width:0}.metrics{grid-template-columns:1fr 1fr}.metric{border-top:1px solid var(--line)}.priority-grid{grid-template-columns:1fr}.readiness-panel #readiness{grid-template-columns:1fr 1fr}.table-tools{width:100%;flex-wrap:wrap}.table-tools input{width:100%}.inventory-panel .section-title{align-items:flex-start;flex-direction:column}.chart-layout{grid-template-columns:1fr}.chart-metrics{border-left:0}.header-meta{flex-wrap:wrap}}
.metrics{grid-template-columns:repeat(6,minmax(0,1fr))}.channel-low{padding:1px 4px;border-radius:3px;background:#fff7e9;color:var(--amber)}.legacy-low-badge{display:inline-flex;margin-top:4px;padding:2px 5px;border-radius:3px;background:#fff7e9;color:var(--amber);font-size:9px;font-weight:650}
`;

const DASHBOARD_CLIENT_JS_BASE = `
let csrf='';
let connectionFailed=false;
let reconnectTimer;
let currentOverview=null;
let selectedShopId=localStorage.getItem('bpa-selected-shop-id')||'';
const q=selector=>document.querySelector(selector);
const qa=selector=>Array.from(document.querySelectorAll(selector));
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const attr=value=>esc(value).replace(/\\n/g,' ');
const dt=value=>value?new Date(value).toLocaleString('zh-CN',{hour12:false}):'无数据';
const pct=value=>value==null?'—':(Number(value)*100).toFixed(1)+'%';
const horizon=(forecast,hours)=>((forecast?.horizons||[]).find(item=>item.hours===hours)||{});
const legacyLowChannels=products=>(products||[]).flatMap(product=>(product.skus||[]).flatMap(sku=>(sku.channels||[]).filter(channel=>Number(channel.stock)<200)));
const productHasLegacyLowChannel=product=>legacyLowChannels([product]).length>0;
const recoveryStateName=value=>({running:'巡检运行中',succeeded:'巡检正常',degraded:'巡检部分失败',auth_required:'抖店登录失效',interrupted:'巡检已中断'}[value]||'等待首轮巡检');
const recoveryReadinessHtml=recovery=>'<div class="readiness-item"><span>自动巡检</span><strong class="'+(recovery?.state==='succeeded'?'severity-normal':recovery?.state==='auth_required'?'severity-critical':'severity-warning')+'">'+esc(recoveryStateName(recovery?.state))+'</strong>'+(recovery?.shopName?'<span>'+esc(recovery.shopName)+'</span>':'')+'</div>';
const recoveryReminderHtml=recovery=>recovery?.state==='auth_required'?'<div class="reminder critical"><span class="reminder-indicator"></span><div><h3>抖店登录会话已失效</h3><p>自动巡检已安全停止，未继续切店或写入数据。</p><span class="reminder-action">重新登录专用 BPA 浏览器后恢复</span></div></div>':'';
const channelAttentionSignals=products=>{const grouped=new Map();for(const product of products||[]){for(const sku of product.skus||[]){const dailyP90=Number(sku.forecast?.daily_p90||0);for(const channel of sku.channels||[]){const key=[product.shop_id,product.product_id,channel.channelGoodsId].join(':');const current=grouped.get(key)||{shopId:product.shop_id,shopName:product.shop_name,productId:product.product_id,productTitle:product.product_title,channelGoodsId:channel.channelGoodsId,stock:Number(channel.stock),dailyP90:0};current.stock=Math.min(current.stock,Number(channel.stock));current.dailyP90+=dailyP90;grouped.set(key,current);}}}return [...grouped.values()].filter(item=>item.stock<50&&item.dailyP90>=50).sort((left,right)=>left.stock-right.stock||right.dailyP90-left.dailyP90);};
const channelAttentionHtml=products=>{const signals=channelAttentionSignals(products);if(!signals.length)return '';return '<div class="queue-note">渠道冷启动关注：库存低于 50 且关联 SKU 日 P90 合计不低于 50。该层用于历史不足 3 天时先发现问题，运营确认后再升级；不写入正式预测事件。</div><table class="risk-table"><thead><tr><th>等级</th><th>店铺</th><th>商品</th><th>渠道品 ID</th><th>渠道库存</th><th>SKU 日 P90 合计</th></tr></thead><tbody>'+signals.slice(0,30).map(item=>'<tr><td><span class="severity-pill warning">冷启动关注</span></td><td><span class="shop-label">'+esc(item.shopName||item.shopId)+'</span></td><td><div class="risk-title">'+esc(productTitle(item.productTitle,item.productId))+'</div><div class="product-sub"><code>'+esc(item.productId)+'</code>'+copyButton(item.productId)+'</div></td><td><code>'+esc(item.channelGoodsId)+'</code>'+copyButton(item.channelGoodsId)+'</td><td><strong class="severity-warning">'+esc(item.stock)+'</strong></td><td>'+esc(item.dailyP90.toFixed(1))+'</td></tr>').join('')+'</tbody></table>';};
const severityName=value=>({critical:'严重',warning:'预警',unknown:'待确认',normal:'正常'}[value]||value);
const kindName=value=>({sku:'SKU',channel:'渠道',reserve:'未占用库存',data_quality:'数据质量'}[value]||value||'风险');
const RISK_REASON_NAMES={
  'Inventory data is stale or incomplete; deterministic risk was suppressed.':'库存快照已过期或数据不完整，系统已停止确定性风险判断。',
  'Recent orders exceed 120 minutes or the latest complete historical order day exceeds 36 hours; deterministic risk was suppressed.':'订单数据源已过期：近期订单超过 2 小时，或历史完整日超过 36 小时。确定性库存风险判断已暂停。',
  'Channel history has not reached the cold-start coverage gate.':'渠道库存历史尚未达到冷启动门槛（至少 3 天且有效快照覆盖率不低于 80%）。',
  'Channel consumption estimate is unavailable.':'渠道消耗估算暂不可用，当前仅展示 SKU 级判断。',
  'Channel mapping exists but no reliable consumption share is available.':'渠道映射已识别，但暂时无法得到可靠的渠道消耗份额。',
  'SKU forecast is missing.':'该 SKU 暂无可用销量预测。',
  'SKU stock covers the six-hour P90 demand.':'SKU 库存可覆盖未来 6 小时 P90 需求。',
  'Channel stock covers the six-hour allocated P90 demand.':'渠道库存可覆盖未来 6 小时分配后的 P90 需求。',
  'Unoccupied reserve can cover all channel top-up deficits for 24 hours.':'未占用库存可覆盖全部渠道未来 24 小时补足缺口。'
};
const riskReason=value=>{
  const text=String(value||'');
  if(RISK_REASON_NAMES[text])return RISK_REASON_NAMES[text];
  let match=text.match(/^SKU stock does not cover the (2|6)-hour P90 demand\.$/);
  if(match)return 'SKU 库存无法覆盖未来 '+match[1]+' 小时 P90 需求。';
  match=text.match(/^Channel stock does not cover the (2|6)-hour allocated P90 demand\.$/);
  if(match)return '渠道库存无法覆盖未来 '+match[1]+' 小时分配后的 P90 需求。';
  match=text.match(/^Unoccupied reserve cannot cover all channel top-up deficits for (6|24) hours\.$/);
  if(match)return '未占用库存无法覆盖全部渠道未来 '+match[1]+' 小时补足缺口。';
  if(/[A-Za-z]{4}/.test(text))return '风险证据待确认（原始诊断已隐藏）。';
  return text||'等待风险证据';
};
const productTitle=(value,productId)=>{const cleaned=String(value||'')
  .replace(/现货模式预览复制链接$/,'')
  .replace(/^0暂无评价设置优惠提升购买转化设置优惠券提升销量/,'')
  .trim();return cleaned&&!(cleaned.includes('售卖中')&&cleaned.length<=24)
    ?cleaned
    :'商品 '+String(productId||'');};
const GOODS_URL='https://fxg.jinritemai.com/ffa/g/list';

class ApiError extends Error{constructor(message,status,code){super(message);this.name='ApiError';this.status=status;this.code=code;}}
const sonner=(()=>{const entries=new Map();let region;const ensure=()=>{if(region)return region;region=document.createElement('div');region.className='sonner-region';region.setAttribute('role','region');region.setAttribute('aria-live','polite');document.body.appendChild(region);return region;};const dismiss=id=>{const entry=entries.get(id);if(!entry)return;clearTimeout(entry.timer);entry.element.classList.add('leaving');entries.delete(id);setTimeout(()=>entry.element.remove(),160);};const show=options=>{const id=options.id||('notice-'+Date.now()+'-'+Math.random().toString(16).slice(2));dismiss(id);const element=document.createElement('div');element.className='sonner-toast '+(options.type||'info');const icon=document.createElement('span');icon.className='sonner-icon';icon.textContent=({success:'✓',error:'!',warning:'△',loading:'…'}[options.type]||'i');const copy=document.createElement('div');copy.className='sonner-copy';const title=document.createElement('strong');title.textContent=options.title;copy.appendChild(title);if(options.description){const description=document.createElement('p');description.textContent=options.description;copy.appendChild(description);}const close=document.createElement('button');close.className='sonner-close';close.type='button';close.textContent='×';close.addEventListener('click',()=>dismiss(id));element.append(icon,copy,close);ensure().appendChild(element);const duration=options.duration===0?0:(options.duration||4000);const timer=duration?setTimeout(()=>dismiss(id),duration):undefined;entries.set(id,{element,timer});return id;};return{show,dismiss};})();
window.sonner=sonner;

async function api(path,options){let response;try{response=await fetch(path,options);}catch{throw new ApiError('连接暂时中断',0,'NETWORK_ERROR');}const contentType=response.headers.get('content-type')||'';let payload;try{payload=contentType.includes('application/json')?await response.json():await response.text();}catch{payload=undefined;}if(!response.ok){const code=payload&&typeof payload==='object'?payload.error:undefined;if(response.status===401)throw new ApiError('访问会话已过期',401,code||'SESSION_REQUIRED');if(response.status===403)throw new ApiError(code==='CSRF_INVALID'?'页面安全令牌已失效':'启动链接无效或已经使用',403,code||'FORBIDDEN');throw new ApiError('服务暂时无法完成请求',response.status,code||('HTTP_'+response.status));}return payload;}
function scheduleReconnect(){if(reconnectTimer)return;reconnectTimer=setTimeout(()=>{reconnectTimer=undefined;load().catch(error=>reportError(error,'自动重连'));},5000);}
function reportError(error,context){const message=error instanceof Error?error.message:String(error);const code=error&&typeof error==='object'&&'code' in error?error.code:'UNEXPECTED_ERROR';const network=code==='NETWORK_ERROR';const recovery=code==='SESSION_REQUIRED'?'请重新打开服务生成的一次性访问地址。':network?'系统将在 5 秒后自动重试，SSH 隧道由后台守护。':'';connectionFailed=true;q('#status').className='status-chip '+(network?'warning':'critical');q('#status').textContent=network?'连接中断 · 自动重试中':message;sonner.dismiss('manual-refresh');sonner.show({id:'global-error',type:network?'warning':'error',title:network?'库存服务连接中断':message,description:[context,code,recovery].filter(Boolean).join(' · '),duration:0});if(network)scheduleReconnect();}

function copyButton(value,label='复制'){return '<button class="inline-copy" data-copy-id="'+attr(value)+'" type="button">'+esc(label)+'</button>';}
function dataQualityFinding(incident){const findings=Array.isArray(incident.findings)?incident.findings:[];return findings.find(item=>item.kind==='data_quality');}
function dataQualityGroups(items){const groups=new Map();items.forEach(incident=>{const finding=dataQualityFinding(incident);if(!finding)return;const reason=riskReason(finding.reason),current=groups.get(reason)||{reason,count:0,lastSeen:null};current.count+=1;if(!current.lastSeen||String(incident.last_seen_at)>String(current.lastSeen))current.lastSeen=incident.last_seen_at;groups.set(reason,current);});return [...groups.values()];}
function incidentTable(items,products,shopId){if(!items.length)return '<div class="empty"><strong>当前无开放风险</strong>风险事件将在此处按严重程度排序。</div>';const groups=dataQualityGroups(items),riskItems=items.filter(incident=>!dataQualityFinding(incident)),summary=groups.map(group=>'<div class="quality-summary"><i></i><div><strong>订单数据质量阻断</strong><p>'+esc(group.reason)+' 最近检查 '+esc(dt(group.lastSeen))+'</p></div><span>影响 '+esc(group.count)+' 个商品</span></div>').join('');if(!riskItems.length)return summary;return summary+'<table class="risk-table"><thead><tr><th>等级</th><th>商品</th><th>影响范围</th><th>判断依据</th><th>更新时间</th><th></th></tr></thead><tbody>'+riskItems.map(incident=>{const findings=Array.isArray(incident.findings)?incident.findings:[];const finding=findings.find(item=>item.severity===incident.severity&&item.kind!=='data_quality')||findings.find(item=>item.severity===incident.severity)||findings[0]||{};const scope=finding.scope||{};const productId=scope.productId||incident.product_id;const product=(products||[]).find(item=>String(item.product_id)===String(productId));const title=productTitle(incident.product_title||product?.product_title,productId);const ids=[['商品',productId],['SKU',scope.platformSkuId],['编码',scope.merchantCode],['渠道',scope.channelGoodsId]].filter(item=>item[1]!=null&&String(item[1]).trim());return '<tr><td><span class="severity-pill '+esc(incident.severity)+'">'+esc(severityName(incident.severity))+'</span></td><td><div class="risk-title">'+esc(title)+'</div><div class="product-sub"><code>'+esc(productId)+'</code>'+copyButton(productId)+'</div></td><td><div class="id-list">'+ids.map(item=>'<div>'+esc(item[0])+' <code>'+esc(item[1])+'</code>'+copyButton(item[1])+'</div>').join('')+'</div></td><td><div class="risk-reason">'+esc(kindName(finding.kind))+' · '+esc(riskReason(finding.reason))+'</div></td><td>'+esc(dt(incident.last_seen_at))+'</td><td><button data-review="'+attr(incident.incident_id)+'" type="button">记录</button></td></tr>';}).join('')+'</tbody></table>';}
function remindersHtml(items){q('#reminderCount').textContent=String(items.length);return items.length?items.map(item=>'<div class="reminder '+esc(item.severity)+'"><span class="reminder-indicator"></span><div><h3>'+esc(item.title)+'</h3><p>'+esc(item.detail)+'</p><span class="reminder-action">'+esc(item.action)+'</span></div></div>').join(''):'<div class="empty"><strong>无待处理事项</strong>数据质量与运行状态均未触发提醒。</div>';}
function readinessHtml(data){const freshness=data.freshness||{},cold=data.coldStart||{},feishu=data.notifications?.feishu||{},all=data.selectedShop?.id==='all',total=Number(cold.totalOrderSkus||0),width=value=>total?Math.max(0,Number(value||0)/total*100):0;return '<div class="readiness-item"><span>'+(all?'全店最旧库存快照':'库存快照')+'</span><strong>'+dt(freshness.latestInventoryAt)+'</strong></div><div class="readiness-item"><span>'+(all?'全店最旧近期订单':'近期订单')+'</span><strong>'+dt(freshness.latestOrderAt)+'</strong></div><div class="readiness-item"><span>历史完整日</span><strong>'+dt(freshness.historicalCompleteThrough)+'</strong></div><div class="readiness-item"><span>新鲜商品 / 全量</span><strong>'+esc(data.counts.freshProducts||0)+' / '+esc(data.counts.products)+'</strong></div><div class="readiness-item"><span>数据商品 / SKU</span><strong>'+esc(data.counts.products)+' / '+esc(data.counts.skus)+'</strong></div><div class="readiness-item"><span>飞书日报</span><strong>'+(feishu.lastSentAt?dt(feishu.lastSentAt):'待首次回报')+'</strong></div><div class="readiness-item wide"><span>预测冷启动分层</span><div class="bar"><i style="width:'+width(cold.directModel)+'%"></i><i style="width:'+width(cold.hierarchicalFallback)+'%"></i><i style="width:'+width(cold.storeBaseline)+'%"></i></div><div class="bar-legend"><span>直接建模 '+esc(cold.directModel||0)+'</span><span>分层回退 '+esc(cold.hierarchicalFallback||0)+'</span><span>店铺基线 '+esc(cold.storeBaseline||0)+'</span><span>已映射 '+esc(cold.inventoryMappedSkus||0)+'</span></div></div>';}
function chart(backtest){if(!backtest||backtest.status!=='ready'||!backtest.points?.length)return '<div class="empty"><strong>回测数据不足</strong>积累至少 35 个完整自然日后自动生成。</div>';const points=backtest.points,w=920,h=210,pad={l:42,r:12,t:12,b:26},max=Math.max(...points.flatMap(point=>[point.actual,point.p90]),1)*1.08,xy=(index,value)=>[pad.l+index*(w-pad.l-pad.r)/Math.max(1,points.length-1),pad.t+(max-value)*(h-pad.t-pad.b)/max],path=key=>points.map((point,index)=>{const position=xy(index,Number(point[key]));return(index?'L':'M')+position[0].toFixed(1)+','+position[1].toFixed(1);}).join(' '),area=points.map((point,index)=>xy(index,Number(point.p90)).join(',')).join(' ')+' '+[...points].reverse().map((point,index)=>xy(points.length-1-index,Number(point.p50)).join(',')).join(' '),ticks=[0,.5,1].map(value=>{const y=pad.t+(1-value)*(h-pad.t-pad.b);return '<line x1="'+pad.l+'" y1="'+y+'" x2="'+(w-pad.r)+'" y2="'+y+'" stroke="#eaecf0"/><text x="'+(pad.l-7)+'" y="'+(y+4)+'" text-anchor="end" fill="#667085" font-size="9">'+Math.round(max*value)+'</text>';}).join(''),labels=[0,Math.floor((points.length-1)/2),points.length-1].map(index=>{const position=xy(index,0);return '<text x="'+position[0]+'" y="'+(h-7)+'" text-anchor="middle" fill="#667085" font-size="9">'+esc(points[index].date.slice(5))+'</text>';}).join('');return '<div class="chart-layout"><div class="chart-wrap"><svg viewBox="0 0 '+w+' '+h+'">'+ticks+'<polygon points="'+area+'" fill="#e8f1f833"/><path d="'+path('p90')+'" fill="none" stroke="#a15c07" stroke-width="1.5" stroke-dasharray="4 3"/><path d="'+path('p50')+'" fill="none" stroke="#3b82c4" stroke-width="1.5"/><path d="'+path('actual')+'" fill="none" stroke="#172033" stroke-width="2"/>'+labels+'</svg></div><div class="chart-metrics"><div class="chart-metric"><small>P90 覆盖率</small><strong>'+pct(backtest.metrics.p90Coverage)+'</strong></div><div class="chart-metric"><small>P50 Pinball</small><strong>'+esc(backtest.metrics.p50PinballLoss)+'</strong></div><div class="chart-metric"><small>P90 Pinball</small><strong>'+esc(backtest.metrics.p90PinballLoss)+'</strong></div><div class="chart-metric"><small>WAPE</small><strong>'+pct(backtest.metrics.wape)+'</strong></div></div></div>';}
function skuTable(product){return '<table class="sku-table"><thead><tr><th>商家编码 / SKU ID</th><th>当前</th><th>占用</th><th>未占用</th><th>渠道品 ID / 库存</th><th>日 P50 / P90</th><th>2h / 6h / 24h P90</th><th>模型</th></tr></thead><tbody>'+(product.skus||[]).map(sku=>{const forecast=sku.forecast,h2=horizon(forecast,2),h6=horizon(forecast,6),h24=horizon(forecast,24),channels=(sku.channels||[]).map(channel=>{const legacyLow=Number(channel.stock)<200;return '<div class="id-line'+(legacyLow?' channel-low':'')+'"><code>'+esc(channel.channelGoodsId)+'</code>'+copyButton(channel.channelGoodsId)+'<strong>'+esc(channel.stock)+'</strong>'+(legacyLow?'<span>旧 &lt;200 对照</span>':'')+'</div>';}).join('')||'无';return '<tr><td><strong>'+esc(sku.merchant_code)+'</strong><div class="id-line"><code>'+esc(sku.platform_sku_id)+'</code>'+copyButton(sku.platform_sku_id)+'</div></td><td>'+esc(sku.current_stock)+'</td><td>'+esc(sku.occupied_stock)+'</td><td>'+esc(sku.unoccupied_stock)+'</td><td class="channels">'+channels+'</td><td>'+(forecast?esc(forecast.daily_p50)+' / '+esc(forecast.daily_p90):'<span class="data-unknown">待建模</span>')+'</td><td>'+(forecast?esc(h2.p90)+' / '+esc(h6.p90)+' / '+esc(h24.p90):'—')+'</td><td>'+(forecast?esc(forecast.selected_model)+'<br><span class="data-unknown">'+esc(forecast.confidence)+'</span>':'—')+'</td></tr>';}).join('')+'</tbody></table>';}
function inventoryHtml(products,incidents){if(!products.length)return '<div class="empty"><strong>暂无库存快照</strong>完成采集后将在此处显示。</div>';const riskByProduct=new Map();(incidents||[]).filter(item=>item.state==='open').forEach(item=>riskByProduct.set(String(item.product_id),item.severity));return '<div class="inventory-scroll"><table class="inventory-table"><thead><tr><th>商品</th><th>风险</th><th>SKU 数</th><th>总库存</th><th>映射质量</th><th>操作</th></tr></thead><tbody>'+products.map(product=>{const risk=riskByProduct.get(String(product.product_id))||'normal',title=productTitle(product.product_title,product.product_id);const search=[title,product.product_id,...(product.skus||[]).flatMap(sku=>[sku.platform_sku_id,sku.merchant_code,...(sku.channels||[]).map(channel=>channel.channelGoodsId)])].join(' ').toLowerCase();return '<tr class="product-summary" data-inventory-row data-risk="'+attr(risk)+'" data-search="'+attr(search)+'" data-product-id="'+attr(product.product_id)+'"><td><div class="product-name">'+esc(title)+'</div><div class="product-sub"><code>'+esc(product.product_id)+'</code>'+copyButton(product.product_id)+'</div></td><td><span class="severity-pill '+esc(risk)+'">'+esc(severityName(risk))+'</span></td><td>'+esc((product.skus||[]).length)+'</td><td>'+esc(product.total_stock)+'</td><td><span class="mapping '+esc(product.mapping_confidence)+'">'+esc(product.mapping_confidence)+'</span></td><td><button class="product-toggle" data-toggle-product="'+attr(product.product_id)+'" type="button">展开 SKU</button></td></tr><tr class="product-detail" data-detail-product="'+attr(product.product_id)+'" hidden><td colspan="6">'+skuTable(product)+'</td></tr>';}).join('')+'</tbody></table></div>';}
function incidentTableAllStores(items,products){const actionable=items.filter(item=>item.severity==='critical'||item.severity==='warning'),attention=channelAttentionSignals(products);q('#incidentCount').textContent=String(actionable.length+attention.length);const unknown=items.filter(item=>item.severity==='unknown').length,legacyLow=legacyLowChannels(products).length,note='<div class="queue-note">正式事件 '+esc(actionable.length)+' 个；渠道冷启动关注 '+esc(attention.length)+' 个；另有 '+esc(unknown)+' 个数据待确认项，以及 '+esc(legacyLow)+' 个渠道品命中旧 &lt;200 对照。</div>',attentionHtml=channelAttentionHtml(products);if(!actionable.length)return note+attentionHtml+(attention.length?'':'<div class="empty"><strong>当前无正式严重、预警或冷启动关注商品</strong>系统仍按 30 分钟周期持续评估。</div>');return note+'<table class="risk-table"><thead><tr><th>等级</th><th>店铺</th><th>商品</th><th>判断依据</th><th>更新时间</th><th></th></tr></thead><tbody>'+actionable.map(incident=>{const findings=Array.isArray(incident.findings)?incident.findings:[],finding=findings.find(item=>item.severity===incident.severity&&item.kind!=='data_quality')||findings[0]||{},scope=finding.scope||{},productId=scope.productId||incident.product_id,product=(products||[]).find(item=>String(item.shop_id)===String(incident.shop_id)&&String(item.product_id)===String(productId)),title=productTitle(incident.product_title||product?.product_title,productId);return '<tr><td><span class="severity-pill '+esc(incident.severity)+'">'+esc(severityName(incident.severity))+'</span></td><td><span class="shop-label">'+esc(incident.shop_name||incident.shop_id)+'</span></td><td><div class="risk-title">'+esc(title)+'</div><div class="product-sub"><code>'+esc(productId)+'</code>'+copyButton(productId)+'</div></td><td><div class="risk-reason">'+esc(kindName(finding.kind))+' · '+esc(riskReason(finding.reason))+'</div></td><td>'+esc(dt(incident.last_seen_at))+'</td><td><button data-review="'+attr(incident.incident_id)+'" type="button">记录</button></td></tr>';}).join('')+'</tbody></table>'+attentionHtml;}
function remindersAllStores(items){const statuses=Array.isArray(currentOverview?.shopStatuses)?[...currentOverview.shopStatuses]:[];statuses.sort((left,right)=>(right.operationalCritical+right.critical*2+right.operationalWarning+right.warning)-(left.operationalCritical+left.critical*2+left.operationalWarning+left.warning));const actionable=items.filter(item=>item.severity==='critical'||item.severity==='warning');q('#reminderCount').textContent=String(actionable.length);const statusHtml='<div class="shop-status-list">'+statuses.map(shop=>{const level=shop.critical||shop.operationalCritical?'critical':shop.warning||shop.operationalWarning?'warning':shop.unknown?'unknown':'normal';return '<div class="shop-status"><div><strong>'+esc(shop.name)+'</strong><p>库存 '+dt(shop.latestInventoryAt)+' · 订单 '+dt(shop.latestOrderAt)+'</p></div><div class="shop-numbers"><span class="severity-pill '+level+'">'+esc(severityName(level))+'</span><span class="muted">'+esc(shop.critical)+' / '+esc(shop.warning)+'</span></div></div>';}).join('')+'</div>';const reminderHtml=actionable.slice(0,8).map(item=>'<div class="reminder '+esc(item.severity)+'"><span class="reminder-indicator"></span><div><h3>'+esc(item.title)+' · '+esc(item.shop_name||'全店')+'</h3><p>'+esc(item.detail)+'</p><span class="reminder-action">'+esc(item.action)+'</span></div></div>').join('');return statusHtml+(reminderHtml?'<div class="queue-note">运行提醒</div>'+reminderHtml:'');}
function remindersAllStoresV2(items){const now=Date.now(),validityMs=2*60*60*1000,statuses=Array.isArray(currentOverview?.shopStatuses)?[...currentOverview.shopStatuses]:[],dataState=shop=>{const at=Date.parse(shop.latestInventoryAt||'');if(!Number.isFinite(at)||Number(shop.products||0)===0)return 'missing';return now-at<=validityMs?'valid':'stale';},statusById=new Map(statuses.map(shop=>[String(shop.id),shop]));const actionable=items.filter(item=>(item.severity==='critical'||item.severity==='warning')&&dataState(statusById.get(String(item.shop_id))||{})==='valid'&&!String(item.id||'').includes('backtest-p90-coverage'));q('#reminderCount').textContent=String(actionable.length);statuses.sort((left,right)=>({valid:0,stale:1,missing:2})[dataState(left)]-({valid:0,stale:1,missing:2})[dataState(right)]||(right.critical*2+right.warning)-(left.critical*2+left.warning)||String(left.name).localeCompare(String(right.name),'zh-CN'));const statusHtml='<div class="shop-status-list">'+statuses.map(shop=>{const state=dataState(shop),valid=state==='valid',missing=state==='missing',level=missing||state==='stale'?'unknown':shop.critical?'critical':shop.warning?'warning':shop.unknown?'unknown':'normal',label=missing?'无数据':state==='stale'?'已入库待刷新':shop.critical?'严重':shop.warning?'预警':shop.unknown?'数据不足':'数据有效',inventory=shop.latestInventoryAt?dt(shop.latestInventoryAt):'无数据',orders=shop.latestOrderAt?dt(shop.latestOrderAt):'无数据';return '<div class="shop-status"><div><strong>'+esc(shop.name)+'</strong><p>库存 '+esc(inventory)+' · 订单 '+esc(orders)+'</p></div><div class="shop-numbers"><span class="severity-pill '+level+'">'+esc(label)+'</span>'+(valid?'<span class="muted">'+esc(shop.critical)+' / '+esc(shop.warning)+'</span>':'')+'</div></div>';}).join('')+'</div>';const reminderHtml=actionable.slice(0,8).map(item=>'<div class="reminder '+esc(item.severity)+'"><span class="reminder-indicator"></span><div><h3>'+esc(item.title)+' · '+esc(item.shop_name||'全店')+'</h3><p>'+esc(item.detail)+'</p><span class="reminder-action">'+esc(item.action)+'</span></div></div>').join('');return statusHtml+(reminderHtml?'<div class="queue-note">2 小时有效数据运行提醒</div>'+reminderHtml:'');}
function inventoryAllStores(products,incidents){if(!products.length)return '<div class="empty"><strong>暂无库存快照</strong>完成采集后将在此处显示。</div>';const riskByProduct=new Map(),validityMs=2*60*60*1000;(incidents||[]).filter(item=>item.state==='open').forEach(item=>riskByProduct.set(String(item.shop_id)+':'+String(item.product_id),item.severity));return '<div class="inventory-scroll"><table class="inventory-table"><thead><tr><th class="shop-column">店铺</th><th>商品</th><th>风险</th><th>SKU 数</th><th>总库存</th><th>映射质量</th><th>操作</th></tr></thead><tbody>'+products.map(product=>{const key=String(product.shop_id)+':'+String(product.product_id),observedAt=Date.parse(product.observed_at||''),fresh=Number.isFinite(observedAt)&&Date.now()-observedAt<=validityMs,risk=fresh?(riskByProduct.get(key)||'normal'):'unknown',riskLabel=fresh?severityName(risk):'待刷新',legacyLow=productHasLegacyLowChannel(product),title=productTitle(product.product_title,product.product_id),search=[product.shop_name,title,product.product_id,...(product.skus||[]).flatMap(sku=>[sku.platform_sku_id,sku.merchant_code,...(sku.channels||[]).map(channel=>channel.channelGoodsId)])].join(' ').toLowerCase(),detailId=product.shop_id+':'+product.product_id;return '<tr class="product-summary" data-inventory-row data-risk="'+attr(risk)+'" data-search="'+attr(search)+'" data-product-id="'+attr(detailId)+'"><td><span class="shop-label">'+esc(product.shop_name||product.shop_id)+'</span></td><td><div class="product-name">'+esc(title)+'</div><div class="product-sub"><code>'+esc(product.product_id)+'</code>'+copyButton(product.product_id)+'</div></td><td><span class="severity-pill '+esc(risk)+'">'+esc(riskLabel)+'</span>'+(legacyLow?'<span class="legacy-low-badge">渠道 &lt;200 对照</span>':'')+'</td><td>'+esc((product.skus||[]).length)+'</td><td>'+esc(product.total_stock)+'</td><td><span class="mapping '+esc(product.mapping_confidence)+'">'+esc(product.mapping_confidence)+'</span></td><td><button class="product-toggle" data-toggle-product="'+attr(detailId)+'" type="button">展开 SKU</button></td></tr><tr class="product-detail" data-detail-product="'+attr(detailId)+'" hidden><td colspan="7">'+skuTable(product)+'</td></tr>';}).join('')+'</tbody></table></div>';}
function rulesHtml(rules){const labels={skuChannelCritical:'SKU / 渠道严重',skuChannelWarning:'SKU / 渠道预警',reserveCritical:'未占用库存严重',reserveWarning:'未占用库存预警',legacyComparison:'旧规则对照'};return '<div class="rule"><small>策略版本</small><strong>'+esc(rules.policyVersion||'—')+'</strong></div>'+Object.keys(labels).map(key=>'<div class="rule"><small>'+esc(labels[key])+'</small><strong>'+esc(rules[key]||'—')+'</strong></div>').join('');}
function scheduleStatusName(value){return({succeeded:'成功',partial:'部分成功',degraded:'降级完成',blocked:'安全阻断',failed:'失败',running:'运行中',skipped:'已跳过'})[value]||value||'未知';}
function scheduleDiagnostic(value){const text=String(value||'');const recovered=text.match(/recovered ([0-9]+) fresh persisted snapshots/);if(recovered)return '浏览器采集未完整结束，已恢复 '+recovered[1]+' 个新鲜快照进入模型。';const partial=text.match(/failed after persisting ([0-9]+) product snapshots/);if(partial)return '本轮采集部分完成：已入库 '+partial[1]+' 个商品，并继续风险评估。';if(text.includes('BROWSER_PAGE_NOT_FOUND:doudian_orders_browser'))return '近期订单页未绑定，已自动回退到历史订单同步。';if(text.includes('"status":"failed"')&&text.includes('"runId"'))return '近期订单读取未完成；库存采集与风险计算已继续完成。';if(text.includes('Inventory browser binding is not configured'))return '该店铺尚未绑定浏览器，仅完成订单同步。';if(text.includes('sales demand refresh ended'))return '订单同步未完整结束，本轮未生成确定性风险。';if(text.includes('connect ENOENT')&&text.includes('core.sock'))return 'Core 重启窗口内暂不可用，本轮已安全跳过。';if(text.includes('previous collection still holds'))return '上一轮仍在运行，本轮已安全跳过。';if(/Risk workflow .* ended/u.test(text))return '单商品风险评估未完成，后续周期将自动重试。';if(text.includes('INVENTORY_SNAPSHOT_OUTPUT_EMPTY'))return '本轮未产生可用库存快照。';if(text.includes('INVENTORY_SNAPSHOT_WORKFLOW_FAILED'))return '库存采集未完整结束。';if(text.includes('INVENTORY_SNAPSHOT_WORKFLOW_UNCERTAIN'))return '库存采集因店铺或页面状态不确定而停止。';return text;}
function schedulesHtml(items){const stores=items.some(item=>item.shop_name);return items.length?'<table class="schedule-table"><thead><tr>'+(stores?'<th>店铺</th>':'')+'<th>计划时间</th><th>状态</th><th>组件</th><th>诊断</th></tr></thead><tbody>'+items.map(item=>'<tr>'+(stores?'<td>'+esc(item.shop_name||item.shop_id||'—')+'</td>':'')+'<td>'+dt(item.scheduled_for)+'</td><td>'+esc(scheduleStatusName(item.status))+'</td><td>'+esc((item.components||item.workflow_runs||[]).length)+'</td><td>'+esc((item.diagnostics||[]).map(scheduleDiagnostic).join('；')||'无')+'</td></tr>').join('')+'</tbody></table>':'<div class="empty">暂无调度记录</div>';}
function applyInventoryFilter(){const term=(q('#inventorySearch')?.value||'').trim().toLowerCase(),filter=q('#inventoryFilter')?.value||'all';qa('[data-inventory-row]').forEach(row=>{const matchesTerm=!term||(row.dataset.search||'').includes(term),matchesFilter=filter==='all'||(filter==='risk'?row.dataset.risk!=='normal':row.dataset.risk===filter),visible=matchesTerm&&matchesFilter;row.hidden=!visible;const detail=q('[data-detail-product="'+CSS.escape(row.dataset.productId||'')+'"]');if(!visible&&detail)detail.hidden=true;});const visible=qa('[data-inventory-row]').filter(row=>!row.hidden).length;q('#inventoryResultCount').textContent='显示 '+visible+' / '+qa('[data-inventory-row]').length+' 个商品';}
function prepareLayout(){const brand=q('.brand>div');if(brand&&!q('#shopMeta'))brand.insertAdjacentHTML('beforeend','<div class="header-meta" id="shopMeta"><span id="shopName">店铺加载中</span><code id="shopId">—</code><span id="generatedAt"></span></div>');const inventoryHeader=q('.inventory-panel .section-title');if(inventoryHeader)inventoryHeader.innerHTML='<div><h2>商品库存明细</h2><div class="inventory-heading-meta"><span id="inventoryUpdated"></span> · <span id="inventoryResultCount"></span></div></div><div class="table-tools"><input id="inventorySearch" type="search" placeholder="搜索商品名、商品 ID、SKU、商家编码或渠道品 ID"><select id="inventoryFilter"><option value="all">全部商品</option><option value="risk">有风险</option><option value="critical">严重</option><option value="warning">预警</option><option value="unknown">待确认</option></select></div>';}
async function load(options={}){q('#status').className='status-chip';q('#status').innerHTML='<i></i>读取中';const query=selectedShopId?'?shopId='+encodeURIComponent(selectedShopId):'';const data=await api('/api/overview'+query);if(reconnectTimer){clearTimeout(reconnectTimer);reconnectTimer=undefined;}currentOverview=data;selectedShopId=data.selectedShop?.id||data.shopId||selectedShopId;if(selectedShopId)localStorage.setItem('bpa-selected-shop-id',selectedShopId);const shopSelect=q('#shopSelect');if(shopSelect){const shops=Array.isArray(data.shops)?data.shops:[];shopSelect.innerHTML=shops.map(shop=>'<option value="'+attr(shop.id)+'"'+(String(shop.id)===String(selectedShopId)?' selected':'')+'>'+esc(shop.name)+'</option>').join('');}const freshness=data.freshness||{},reminders=data.reminders||[],incidents=data.incidents||[],open=incidents.filter(item=>item.state==='open'),critical=open.filter(item=>item.severity==='critical').length,warning=open.filter(item=>item.severity==='warning').length,unknown=open.filter(item=>item.severity==='unknown').length,operationalCritical=reminders.filter(item=>item.severity==='critical').length,operationalWarning=reminders.filter(item=>item.severity==='warning').length;q('#status').className='status-chip '+(critical||operationalCritical?'critical':warning||operationalWarning?'warning':'');q('#status').innerHTML='<i></i>'+(critical||operationalCritical?'存在严重事项':warning||operationalWarning?'存在预警':'运行正常');q('#shopName').textContent=data.selectedShop?.name||'店铺';q('#shopId').textContent=selectedShopId||'—';q('#generatedAt').textContent='更新 '+dt(data.generatedAt);q('#metrics').innerHTML=[['严重风险',critical,'开放事件',critical?'critical':'good'],['预警风险',warning,'开放事件',warning?'warning':'good'],['待确认',unknown,'数据质量事件',unknown?'warning':'good'],['商品 / SKU',data.counts.products+' / '+data.counts.skus,'已入库范围',''],['P90 覆盖率',pct(data.backtest?.metrics?.p90Coverage),'目标 85%–95%',data.backtest?.status==='ready'?'good':'warning']].map(item=>'<article class="metric '+item[3]+'"><small>'+esc(item[0])+'</small><strong>'+esc(item[1])+'</strong><span>'+esc(item[2])+'</span></article>').join('');q('#incidentCount').textContent=String(open.length);q('#incidents').innerHTML=incidentTable(open,data.products||[],selectedShopId);q('#reminders').innerHTML=remindersHtml(reminders);q('#readiness').innerHTML=readinessHtml(data);q('#backtest').innerHTML=chart(data.backtest);q('#inventoryUpdated').textContent='快照 '+dt(freshness.latestInventoryAt);q('#products').innerHTML=inventoryHtml(data.products||[],open);q('#rules').innerHTML=rulesHtml(data.rules||{});q('#schedules').innerHTML=schedulesHtml(data.schedules||[]);applyInventoryFilter();notifyReminders(reminders);sonner.dismiss('global-error');if(connectionFailed){sonner.show({id:'connection-restored',type:'success',title:'连接已恢复',description:'看板数据已重新同步。'});connectionFailed=false;}if(options.manual)sonner.show({id:'manual-refresh',type:'success',title:'刷新完成',description:'已载入 '+data.counts.products+' 个商品、'+data.counts.skus+' 个 SKU。'});}
function setNotifyButton(){const button=q('#enableNotify');if(!('Notification' in window)){button.textContent='桌面提醒不可用';button.disabled=true;return;}if(Notification.permission==='granted')button.textContent='桌面提醒已开启';else if(Notification.permission==='denied')button.textContent='桌面提醒已关闭';}
async function enableNotifications(){if(!('Notification' in window))return;const permission=await Notification.requestPermission();setNotifyButton();sonner.show({type:permission==='granted'?'success':'warning',title:permission==='granted'?'桌面提醒已开启':'桌面提醒未开启'});}
function notifyReminders(items){if(!('Notification' in window)||Notification.permission!=='granted')return;(items||[]).filter(item=>item.severity==='critical'||item.severity==='warning').forEach(item=>{const key='bpa-reminder:'+item.id;if(localStorage.getItem(key))return;new Notification(item.title,{body:item.detail,tag:item.id});localStorage.setItem(key,new Date().toISOString());});}
async function copyId(button){const value=button.dataset.copyId||'';try{await navigator.clipboard.writeText(value);}catch{const input=document.createElement('textarea');input.value=value;document.body.appendChild(input);input.select();document.execCommand('copy');input.remove();}sonner.show({type:'success',title:'已复制',description:value,duration:1600});}
async function heartbeat(){try{const session=await bootstrapSession();csrf=session.csrf;}catch(error){reportError(error,'会话心跳');}}
async function bootstrapSession(){const token=new URLSearchParams(location.hash.slice(1)).get('token');if(token){sessionStorage.setItem('bpa-inventory-access-token',token);history.replaceState(null,'',location.pathname);}try{return await api('/api/session');}catch(error){if(error?.code!=='SESSION_REQUIRED')throw error;const saved=sessionStorage.getItem('bpa-inventory-access-token');if(!saved)throw new Error('请使用局域网安全入口重新进入');return api('/api/session',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token:saved})});}}
async function init(){prepareLayout();const session=await bootstrapSession();csrf=session.csrf;setNotifyButton();await load();setInterval(()=>{if(document.visibilityState==='visible')load().catch(error=>reportError(error,'自动刷新'));},60000);setInterval(()=>void heartbeat(),300000);}
document.addEventListener('click',event=>{const target=event.target instanceof Element?event.target:null,copy=target?.closest('[data-copy-id]'),review=target?.closest('[data-review]'),toggle=target?.closest('[data-toggle-product]');if(copy){void copyId(copy);return;}if(review){q('#incidentId').value=review.dataset.review;q('#review').showModal();return;}if(toggle){const detail=q('[data-detail-product="'+CSS.escape(toggle.dataset.toggleProduct||'')+'"]');if(detail){detail.hidden=!detail.hidden;toggle.textContent=detail.hidden?'展开 SKU':'收起 SKU';}}});
document.addEventListener('input',event=>{if(event.target?.id==='inventorySearch')applyInventoryFilter();});
document.addEventListener('change',event=>{if(event.target?.id==='inventoryFilter')applyInventoryFilter();if(event.target?.id==='shopSelect'){selectedShopId=event.target.value;sonner.show({id:'shop-switch',type:'loading',title:'正在切换店铺',duration:0});load().then(()=>sonner.show({id:'shop-switch',type:'success',title:'店铺数据已更新'})).catch(error=>reportError(error,'切换店铺'));}});
q('#reload').addEventListener('click',()=>{sonner.show({id:'manual-refresh',type:'loading',title:'正在刷新数据',duration:0});load({manual:true}).catch(error=>reportError(error,'手动刷新'));});
q('#enableNotify').addEventListener('click',()=>enableNotifications().catch(error=>reportError(error,'桌面提醒')));
q('#submit').addEventListener('click',async event=>{event.preventDefault();try{await api('/api/reviews',{method:'POST',headers:{'content-type':'application/json','x-csrf-token':csrf},body:JSON.stringify({incidentId:q('#incidentId').value,decision:q('#decision').value,note:q('#note').value})});q('#review').close();sonner.show({type:'success',title:'处理结果已保存'});await load();}catch(error){reportError(error,'保存处理结果');}});
window.addEventListener('unhandledrejection',event=>reportError(event.reason,'未处理异常'));
window.addEventListener('error',event=>reportError(event.error||event.message,'页面运行异常'));
function shopCoverage(){const statuses=Array.isArray(currentOverview?.shopStatuses)?currentOverview.shopStatuses:[],now=Date.now(),available=statuses.filter(shop=>Number(shop.products||0)>0&&shop.latestInventoryAt).length,valid=statuses.filter(shop=>{const at=Date.parse(shop.latestInventoryAt||'');return Number(shop.products||0)>0&&Number.isFinite(at)&&now-at<=2*60*60*1000;}).length;return {available,valid,total:statuses.length,incomplete:statuses.length>0&&valid<statuses.length};}
function updateHero(){const hero=q('#heroAlert'),meta=q('#heroMeta');if(!hero||!meta||!currentOverview)return;const metrics=qa('#metrics .metric strong'),critical=Number(metrics[0]?.textContent||0),warning=Number(metrics[1]?.textContent||0),unknown=Number(metrics[2]?.textContent||0),shops=Array.isArray(currentOverview.shops)?currentOverview.shops.length:1,coverage=shopCoverage();meta.textContent=shops+' 家店铺 · 当前店 '+Number(currentOverview.counts?.products||0)+' 个商品 · '+Number(currentOverview.counts?.skus||0)+' 个 SKU';const level=critical?'critical':warning?'warning':unknown||coverage.incomplete?'unknown':'normal';hero.className='hero-alert '+level;hero.innerHTML='<i></i><strong>'+(critical?critical+' 个严重风险需要立即处理':warning?warning+' 个预警风险需要关注':unknown?unknown+' 个数据项待确认':coverage.incomplete?coverage.valid+' / '+coverage.total+' 家店铺数据在 2 小时有效期内':'当前没有开放的确定性库存风险')+'</strong><span>'+(critical||warning?'按预计耗尽时间优先处置':unknown?'待确认不等同于缺货风险':coverage.incomplete?'已入库 '+coverage.available+' 家；过期数据保留展示并等待刷新':'库存、订单与模型链路持续运行')+'</span>';}
function syncCommandCenterStatus(){const metrics=qa('#metrics .metric strong'),critical=Number(metrics[0]?.textContent||0),warning=Number(metrics[1]?.textContent||0),unknown=Number(metrics[2]?.textContent||0),coverage=shopCoverage(),status=q('#status');if(!status)return;status.className='status-chip '+(critical?'critical':warning||unknown||coverage.incomplete?'warning':'');status.innerHTML='<i></i>'+(critical?'存在严重风险':warning?'存在预警风险':unknown?'数据待完善':coverage.incomplete?coverage.valid+'/'+coverage.total+' 数据有效':'数据正常');}
const updateHeroFromData=updateHero;
updateHero=()=>{const recovery=currentOverview?.recovery;if(recovery?.state!=='auth_required'){updateHeroFromData();return;}const hero=q('#heroAlert'),meta=q('#heroMeta'),shops=Array.isArray(currentOverview?.shops)?currentOverview.shops.length:1;if(meta)meta.textContent=shops+' 家店铺 · '+Number(currentOverview?.counts?.products||0)+' 个商品 · '+Number(currentOverview?.counts?.skus||0)+' 个 SKU';if(hero){hero.className='hero-alert critical';hero.innerHTML='<i></i><strong>抖店登录失效，自动巡检已安全停止</strong><span>当前快照继续展示；重新登录后自动恢复采集</span>';}};
const syncCommandCenterStatusFromData=syncCommandCenterStatus;
syncCommandCenterStatus=()=>{if(currentOverview?.recovery?.state!=='auth_required'){syncCommandCenterStatusFromData();return;}const status=q('#status');if(status){status.className='status-chip critical';status.innerHTML='<i></i>巡检等待登录';}};
const statusGuard=new MutationObserver(()=>{updateHero();syncCommandCenterStatus();});
statusGuard.observe(q('#metrics'),{childList:true,subtree:true});
init().catch(error=>reportError(error,'会话初始化'));
`;

export const DASHBOARD_CLIENT_JS = DASHBOARD_CLIENT_JS_BASE
  .replace("订单数据质量阻断","数据待确认")
  .replace("let selectedShopId=localStorage.getItem('bpa-selected-shop-id')||'';","let selectedShopId='all';")
  .replace("q('#incidents').innerHTML=incidentTable(open,data.products||[],selectedShopId);","q('#incidents').innerHTML=incidentTableAllStores(open,data.products||[]);")
  .replace("q('#reminders').innerHTML=remindersHtml(reminders);","q('#reminders').innerHTML=recoveryReminderHtml(data.recovery)+remindersAllStoresV2(reminders);")
  .replace("q('#readiness').innerHTML=readinessHtml(data);","q('#readiness').innerHTML=recoveryReadinessHtml(data.recovery)+readinessHtml(data);")
  .replace("q('#products').innerHTML=inventoryHtml(data.products||[],open);","q('#products').innerHTML=inventoryAllStores(data.products||[],open);")
  .replace("data.selectedShop?.name||'店铺'","'全店监测'")
  .replace("['P90 覆盖率',pct(data.backtest?.metrics?.p90Coverage),'目标 85%–95%',data.backtest?.status==='ready'?'good':'warning']","['低库存对照',legacyLowChannels(data.products||[]).length,'渠道库存 < 200（非正式风险）',legacyLowChannels(data.products||[]).length?'warning':'good'],['P90 覆盖率',pct(data.backtest?.metrics?.p90Coverage),'目标 85%–95%',data.backtest?.status==='ready'?'good':'warning']")
  .replace(" 家店铺 · 当前店 "," 家店铺 · ")
  .replace("请重新打开服务生成的一次性访问地址。","请从局域网安全入口重新进入；后续会话将在当前标签页自动续签。");
