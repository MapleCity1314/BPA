export const DASHBOARD_CLIENT_HTML = String.raw`<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="color-scheme" content="light">
  <meta name="theme-color" content="#f4f8f6">
  <title>BPA 库存运营面板</title>
  <link rel="stylesheet" href="/app.css">
</head>
<body>
  <a class="skip-link" href="#main">跳到主要内容</a>
  <header class="appbar">
    <div class="brand"><strong>库存运营面板</strong><span id="connection" class="pill neutral">正在连接</span></div>
    <nav class="actions" aria-label="面板操作">
      <button id="notify" class="text-button" type="button">桌面提醒</button>
      <button id="reload" class="icon-button" type="button" aria-label="手动刷新">↻</button>
      <a class="text-link" href="/technical">技术监控</a>
    </nav>
  </header>
  <div class="shell">
    <aside class="sidebar" aria-label="店铺筛选">
      <label class="search-label" for="search">搜索商品或编号</label>
      <input id="search" name="inventory-search" type="search" placeholder="商品名、商品 ID、SKU、商家编码、渠道品 ID…" autocomplete="off">
      <p id="shopCount" class="section-label">店铺</p>
      <div id="shops" class="shop-list"></div>
    </aside>
    <main id="main" class="main">
      <div class="mobile-shopbar">
        <label for="mobileShop">查看店铺</label>
        <select id="mobileShop" name="mobile-shop"></select>
      </div>
      <section id="hero" class="hero" aria-label="当前经营状态"></section>
      <section id="stats" class="stats" aria-label="经营数据摘要"></section>
      <section class="section" aria-labelledby="incidentHeading">
        <div class="section-head"><h2 id="incidentHeading">风险处理队列</h2><span id="incidentCount" class="count"></span></div>
        <div id="incidents"></div>
        <div id="incidentPager" class="pager"></div>
      </section>
      <section class="section" aria-labelledby="reminderHeading">
        <div class="section-head"><h2 id="reminderHeading">运营提醒</h2><span id="reminderCount" class="count"></span></div>
        <div id="reminders"></div>
      </section>
      <section class="section" aria-labelledby="productHeading">
        <div class="section-head product-head">
          <div><h2 id="productHeading">商品库存</h2><p id="productMeta" class="muted"></p></div>
          <select id="riskFilter" name="risk-filter" aria-label="筛选风险状态">
            <option value="all">全部状态</option>
            <option value="critical">严重风险</option>
            <option value="warning">预警风险</option>
            <option value="unknown">数据待确认</option>
            <option value="normal">正常</option>
          </select>
        </div>
        <div id="products"></div>
        <div id="productPager" class="pager"></div>
      </section>
      <section class="section" aria-labelledby="cycleHeading">
        <div class="section-head"><h2 id="cycleHeading">最近一次正式库存周期</h2></div>
        <div id="productionCycle"></div>
      </section>
    </main>
  </div>
  <dialog id="productDialog" class="drawer"><div id="productDetail"></div></dialog>
  <dialog id="reviewDialog" class="review-dialog">
    <form id="reviewForm" method="dialog">
      <div class="dialog-head"><div><h2>提交运营判断</h2><p id="reviewProduct" class="muted"></p></div><button class="icon-button" type="button" data-close-review aria-label="关闭">×</button></div>
      <input id="incidentId" type="hidden">
      <fieldset><legend>判断结果</legend>
        <label><input type="radio" name="decision" value="valid" checked>确认风险有效</label>
        <label><input type="radio" name="decision" value="false_positive">确认是误报</label>
        <label><input type="radio" name="decision" value="needs_context">信息不足，需要补充上下文</label>
      </fieldset>
      <label for="note">补充说明</label>
      <textarea id="note" name="review-note" maxlength="4000" rows="5" autocomplete="off" placeholder="记录处置动作或需要补充的信息…"></textarea>
      <div class="dialog-actions"><button class="text-button" type="button" data-close-review>取消</button><button id="submitReview" class="primary-button" type="submit">提交判断</button></div>
    </form>
  </dialog>
  <div id="toast" class="toast" role="status" aria-live="polite" hidden></div>
  <script src="/app.js"></script>
</body>
</html>`;

export const DASHBOARD_TECHNICAL_HTML = String.raw`<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#f4f8f6"><title>BPA 库存技术监控</title><link rel="stylesheet" href="/app.css"></head>
<body><a class="skip-link" href="#technical-main">跳到主要内容</a><header class="appbar"><div class="brand"><strong>库存技术监控</strong><span id="connection" class="pill neutral">正在连接</span></div><nav class="actions" aria-label="技术面板操作"><a class="text-link" href="/">返回运营面板</a><button id="reload" class="icon-button" type="button" aria-label="手动刷新">↻</button></nav></header><main id="technical-main" class="technical-main"><section class="section"><div class="section-head"><h1>预测回测</h1></div><div id="technicalMetrics" class="stats"></div><div id="backtest"></div></section><section class="section"><div class="section-head"><h2>冷启动与映射覆盖</h2></div><div id="coldStart"></div></section><section class="section"><div class="section-head"><h2>正式工作流状态</h2></div><pre id="cycleTechnical" class="technical-json"></pre></section></main><div id="toast" class="toast" role="status" aria-live="polite" hidden></div><script src="/technical.js"></script></body></html>`;

export const DASHBOARD_CLIENT_CSS = String.raw`:root{font-family:-apple-system,BlinkMacSystemFont,"SF Pro Text","PingFang SC","Microsoft YaHei",sans-serif;color:#1d2925;background:#f4f8f6;font-synthesis:none;--surface:#fff;--surface-soft:#f7faf8;--ink:#1d2925;--muted:#687670;--line:#dce5e1;--green:#1b6f58;--green-soft:#dcefe8;--red:#aa312d;--red-soft:#f9dedc;--amber:#876400;--amber-soft:#fff0c2;--grey:#e9efec;--shadow:0 10px 28px rgb(21 42 35 / 7%)}*{box-sizing:border-box}body{margin:0;min-width:320px;background:#f4f8f6;color:var(--ink);font-variant-numeric:tabular-nums}button,input,select,textarea{font:inherit}button,a,select,input,textarea{outline-offset:3px;touch-action:manipulation;-webkit-tap-highlight-color:rgb(27 111 88 / 16%)}.skip-link{position:fixed;top:8px;left:8px;z-index:100;transform:translateY(-160%);padding:9px 12px;border-radius:8px;background:var(--green);color:white}.skip-link:focus{transform:translateY(0)}button:focus-visible,a:focus-visible,select:focus-visible,input:focus-visible,textarea:focus-visible{outline:3px solid #6ea995}.appbar{height:64px;position:sticky;top:0;z-index:20;display:flex;align-items:center;justify-content:space-between;padding:0 20px;background:rgb(255 255 255 / 94%);border-bottom:1px solid var(--line);backdrop-filter:blur(12px)}.brand,.actions{display:flex;align-items:center;gap:12px}.brand strong{font-size:20px}.pill,.badge,.count{display:inline-flex;align-items:center;border-radius:999px;padding:4px 9px;font-size:12px;font-weight:650}.pill.neutral,.badge.neutral{background:var(--grey);color:#4f5f59}.pill.normal,.badge.normal{background:var(--green-soft);color:var(--green)}.pill.warning,.badge.warning{background:var(--amber-soft);color:var(--amber)}.pill.critical,.badge.critical{background:var(--red-soft);color:var(--red)}.pill.unknown,.badge.unknown{background:var(--grey);color:#52605b}.actions button,.text-link{min-height:40px}.text-button,.icon-button,.primary-button,.text-link,.copy-button,.page-button{border:0;border-radius:999px;padding:9px 14px;cursor:pointer;text-decoration:none}.text-button,.icon-button,.text-link,.copy-button,.page-button{background:transparent;color:var(--green)}.text-button:hover,.icon-button:hover,.text-link:hover,.copy-button:hover,.page-button:hover{background:var(--green-soft)}.icon-button{font-size:20px;min-width:40px}.primary-button{background:var(--green);color:white}.primary-button:disabled{opacity:.5;cursor:wait}.shell{display:grid;grid-template-columns:280px minmax(0,1fr);gap:16px;max-width:1600px;margin:0 auto;padding:16px}.sidebar{position:sticky;top:80px;align-self:start;display:flex;flex-direction:column;gap:10px;max-height:calc(100vh - 96px);overflow:auto}.search-label,.section-label{font-size:13px;color:var(--muted);font-weight:650;margin:0}.sidebar input,.product-head select,.mobile-shopbar select,.review-dialog textarea{width:100%;border:1px solid #b8c6c0;border-radius:10px;background:white;color:var(--ink);padding:11px 12px}.shop-list{display:flex;flex-direction:column;background:var(--surface);border:1px solid var(--line);border-radius:14px;overflow:hidden}.shop-button{border:0;border-bottom:1px solid var(--line);background:white;text-align:left;padding:12px;cursor:pointer;color:var(--ink)}.shop-button:last-child{border-bottom:0}.shop-button:hover,.shop-button.active{background:var(--green-soft)}.shop-button strong,.shop-button span{display:block}.shop-button span{margin-top:4px;color:var(--muted);font-size:12px}.main{min-width:0;display:flex;flex-direction:column;gap:18px}.mobile-shopbar{display:none}.hero,.section{background:var(--surface);border:1px solid var(--line);border-radius:16px;box-shadow:var(--shadow)}.hero{padding:18px 20px}.hero.critical{background:var(--red-soft);border-color:#efb9b5}.hero.warning{background:var(--amber-soft);border-color:#e9d38f}.hero.normal{background:var(--green-soft);border-color:#b8ded0}.hero h1{font-size:20px;margin:0 0 4px}.hero p{margin:0;color:inherit;opacity:.8}.stats{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:10px}.stat{background:var(--surface);border:1px solid var(--line);border-radius:14px;padding:14px;min-width:0}.stat span,.stat small{display:block;color:var(--muted);font-size:12px}.stat strong{display:block;margin:8px 0 5px;font-size:25px;line-height:1.05}.section{padding:18px}.section-head{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:12px}.section h1,.section h2{margin:0;font-size:17px;text-wrap:balance}.count{background:var(--grey);color:var(--muted)}.muted{color:var(--muted);font-size:13px;margin:4px 0 0}.incident-list,.reminder-list{display:flex;flex-direction:column;gap:10px}.incident-card,.reminder-card{border:1px solid var(--line);border-left-width:4px;border-radius:12px;padding:13px;background:var(--surface-soft)}.incident-card.critical,.reminder-card.critical{border-left-color:var(--red)}.incident-card.warning,.reminder-card.warning{border-left-color:#c89b17}.incident-title{display:flex;align-items:center;gap:8px;flex-wrap:wrap}.incident-title h3,.reminder-card h3{font-size:15px;margin:0}.incident-card p,.reminder-card p{margin:7px 0;color:#43514c;font-size:13px;line-height:1.55}.identifier-list{display:flex;flex-wrap:wrap;gap:7px;margin-top:9px}.copy-button{background:var(--grey);color:#3e4c47;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:12px;padding:6px 9px}.incident-actions{display:flex;justify-content:flex-end}.empty{padding:24px;text-align:center;color:var(--muted);background:var(--surface-soft);border-radius:12px}.product-head{align-items:flex-start}.product-head select{width:auto;min-width:150px}.table-wrap{overflow:auto;border:1px solid var(--line);border-radius:12px}.inventory-table{border-collapse:collapse;width:100%;min-width:900px}.inventory-table th,.inventory-table td{padding:11px 12px;text-align:left;border-bottom:1px solid var(--line);vertical-align:middle}.inventory-table th{position:sticky;top:0;background:#eef4f1;color:#52615b;font-size:12px}.inventory-table td{font-size:13px}.inventory-table tr:last-child td{border-bottom:0}.inventory-table tbody tr:hover{background:#f5faf7}.product-title{font-weight:650}.pager{display:flex;justify-content:center;align-items:center;gap:8px;margin-top:12px;color:var(--muted);font-size:13px}.page-button{border:1px solid var(--line);background:white}.page-button:disabled{opacity:.4;cursor:not-allowed}.cycle-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}.cycle-cell{padding:12px;background:var(--surface-soft);border-radius:10px}.cycle-cell span{display:block;color:var(--muted);font-size:12px}.cycle-cell strong{display:block;margin-top:5px}.drawer{overscroll-behavior:contain;position:fixed;inset:0 0 0 auto;width:min(520px,100%);max-height:none;height:100%;margin:0;border:0;border-left:1px solid var(--line);padding:0;background:var(--surface);color:var(--ink);overflow:auto}.drawer::backdrop,.review-dialog::backdrop{background:rgb(0 0 0 / 38%)}.drawer-body{padding:20px}.dialog-head{position:sticky;top:0;z-index:2;display:flex;justify-content:space-between;align-items:flex-start;gap:12px;background:var(--surface);padding-bottom:12px;border-bottom:1px solid var(--line)}.dialog-head h2{margin:0;font-size:19px}.detail-summary{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin:14px 0}.detail-summary div,.sku-card{background:var(--surface-soft);border:1px solid var(--line);border-radius:12px;padding:12px}.detail-summary span{display:block;color:var(--muted);font-size:12px}.detail-summary strong{display:block;margin-top:5px}.sku-list{display:flex;flex-direction:column;gap:10px}.sku-card h3{margin:0 0 7px;font-size:14px}.sku-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px}.sku-grid div{font-size:13px}.sku-grid span{display:block;color:var(--muted);font-size:11px}.forecast-box{margin-top:10px;padding-top:10px;border-top:1px solid var(--line)}.forecast-box p{margin:5px 0;font-size:13px}.review-dialog{overscroll-behavior:contain;width:min(520px,calc(100% - 24px));border:0;border-radius:16px;padding:20px;color:var(--ink)}.review-dialog form{display:flex;flex-direction:column;gap:14px}.review-dialog fieldset{display:flex;flex-direction:column;gap:10px;border:1px solid var(--line);border-radius:12px;padding:12px}.review-dialog fieldset label{display:flex;gap:8px}.dialog-actions{display:flex;justify-content:flex-end;gap:8px}.toast{position:fixed;left:50%;bottom:22px;z-index:80;transform:translateX(-50%);max-width:min(520px,calc(100% - 24px));padding:10px 16px;border-radius:999px;background:#24342e;color:white;box-shadow:var(--shadow)}.technical-main{max-width:1200px;margin:0 auto;padding:20px;display:flex;flex-direction:column;gap:18px}.technical-json{overflow:auto;background:#1d2925;color:#dff2ea;padding:14px;border-radius:12px;white-space:pre-wrap}.chart{width:100%;height:auto;overflow:visible}.chart line{stroke:#dce5e1}.chart .actual{stroke:#23342e}.chart .p50{stroke:#3c7aa3}.chart .p90{stroke:#b37b14}.chart text{fill:#687670;font-size:11px}@media(max-width:1100px){.stats{grid-template-columns:repeat(3,minmax(0,1fr))}}@media(max-width:839px){.appbar{padding:0 12px}.brand strong{font-size:17px}.actions .text-button{display:none}.shell{display:block;padding:10px}.sidebar{position:static;max-height:none;overflow:visible}.sidebar .search-label,.sidebar #search,.sidebar .section-label,.sidebar .shop-list{display:none}.mobile-shopbar{display:flex;align-items:center;gap:10px;margin-bottom:10px}.mobile-shopbar label{white-space:nowrap;color:var(--muted);font-size:13px}.main{gap:12px}.stats{grid-template-columns:repeat(2,minmax(0,1fr))}.section,.hero{border-radius:12px;padding:14px}.cycle-grid{grid-template-columns:1fr}.drawer{width:100%}}@media(max-width:520px){.appbar{height:auto;min-height:58px;align-items:flex-start;padding-block:10px}.brand{align-items:flex-start;flex-direction:column;gap:5px}.actions{gap:2px}.actions .text-link{font-size:12px;padding:8px}.stats{grid-template-columns:1fr 1fr}.stat strong{font-size:21px}.section-head.product-head{align-items:stretch;flex-direction:column}.product-head select{width:100%}.detail-summary,.sku-grid{grid-template-columns:1fr 1fr}}@media(prefers-reduced-motion:reduce){*,*::before,*::after{scroll-behavior:auto!important;animation-duration:.01ms!important;animation-iteration-count:1!important;transition-duration:.01ms!important}}`;

export const DASHBOARD_CLIENT_JS = String.raw`'use strict';
const q=(selector,root=document)=>root.querySelector(selector);
const qa=(selector,root=document)=>Array.from(root.querySelectorAll(selector));
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const attr=esc;
const PRODUCT_PAGE_SIZE=50;
const INCIDENT_PAGE_SIZE=20;
let csrf='';
let overview=null;
let selectedShopId=new URL(location.href).searchParams.get('shopId')||'all';
let productPage=1;
let incidentPage=1;
let toastTimer;
let seenNotifications=null;
const productByKey=new Map();
function finite(value){const number=Number(value);return Number.isFinite(number)?number:0;}
function dt(value){if(!value||!Number.isFinite(Date.parse(value)))return '尚无数据';return new Date(value).toLocaleString('zh-CN',{hour12:false});}
function relative(value){if(!value||!Number.isFinite(Date.parse(value)))return '尚无数据';const minutes=Math.max(0,Math.floor((Date.now()-Date.parse(value))/60000));if(minutes<1)return '刚刚';if(minutes<60)return minutes+' 分钟前';const hours=Math.floor(minutes/60);if(hours<24)return hours+' 小时前';return Math.floor(hours/24)+' 天前';}
function stale(value){return !value||!Number.isFinite(Date.parse(value))||Date.now()-Date.parse(value)>120*60*1000;}
function showToast(message,tone='normal'){const element=q('#toast');element.textContent=message;element.hidden=false;element.dataset.tone=tone;clearTimeout(toastTimer);toastTimer=setTimeout(()=>{element.hidden=true;},2600);}
async function api(path,options={}){let response;try{response=await fetch(path,{credentials:'same-origin',...options});}catch{const error=new Error('网络连接中断');error.code='NETWORK_ERROR';throw error;}let payload={};try{payload=await response.json();}catch{}if(!response.ok){const error=new Error(payload.message||payload.error||'数据读取失败');error.code=payload.error||'HTTP_'+response.status;throw error;}return payload;}
async function establishSession(){const launchToken=new URLSearchParams(location.hash.slice(1)).get('token');if(launchToken){const result=await api('/api/session',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token:launchToken})});history.replaceState(null,'',location.pathname+location.search);csrf=result.csrf;return;}const result=await api('/api/session');csrf=result.csrf;}
function severityLabel(value){return ({critical:'严重风险',warning:'预警风险',unknown:'数据待确认',normal:'正常'})[value]||'数据待确认';}
function incidentState(value){return ({pending:'待复核',open:'处理中',resolved:'已结束'})[value]||'待确认';}
function recoveryHeadline(data){const recovery=data.recovery||{};if(recovery.state==='auth_required')return {tone:'critical',title:'抖店登录已失效，自动巡检已安全停止',detail:'当前已入库数据继续展示；重新登录后恢复采集。'};if(recovery.state==='interrupted')return {tone:'critical',title:'自动巡检已中断',detail:'库存事实仍可查看，需要技术人员检查采集状态。'};if(recovery.state==='running')return {tone:'warning',title:'自动巡检正在更新',detail:'部分商品可能已先完成更新，请以各商品快照时间为准。'};if(recovery.state==='degraded')return {tone:'warning',title:'最近一轮只完成了部分数据',detail:'已持久化的数据继续展示，未完成部分等待下一轮恢复。'};const critical=(data.incidents||[]).filter(item=>item.state==='open'&&item.severity==='critical').length;const warning=(data.incidents||[]).filter(item=>item.state==='open'&&item.severity==='warning').length;if(critical)return {tone:'critical',title:critical+' 个严重风险需要立即处理',detail:'按预计售罄时间和可调拨库存缺口优先处置。'};if(warning)return {tone:'warning',title:warning+' 个预警风险需要尽快处理',detail:'当前尚未达到严重程度，但需要运营跟进。'};return {tone:'normal',title:'当前没有开放的确定性库存风险',detail:'数据待确认项目不等同于缺货风险。'};}
function updateUrl(){const url=new URL(location.href);if(selectedShopId==='all')url.searchParams.delete('shopId');else url.searchParams.set('shopId',selectedShopId);const search=q('#search').value.trim();if(search)url.searchParams.set('q',search);else url.searchParams.delete('q');history.replaceState(null,'',url);}
function renderShops(){const shops=Array.isArray(overview.shops)?overview.shops:[];const statuses=Array.isArray(overview.shopStatuses)?overview.shopStatuses:[];const byId=new Map(statuses.map(item=>[String(item.id),item]));q('#shopCount').textContent='店铺（共 '+shops.length+' 家）';const aggregate=statuses.reduce((result,item)=>({critical:result.critical+finite(item.critical),warning:result.warning+finite(item.warning)}),{critical:0,warning:0});const all=[{id:'all',name:'全部店铺',critical:aggregate.critical,warning:aggregate.warning},...shops.map(shop=>({id:String(shop.id),name:String(shop.name),...(byId.get(String(shop.id))||{})}))];q('#shops').innerHTML=all.map(shop=>'<button class="shop-button '+(shop.id===selectedShopId?'active':'')+'" type="button" data-shop="'+attr(shop.id)+'"><strong>'+esc(shop.name)+'</strong><span>'+((finite(shop.critical)||finite(shop.warning))?'严重 '+finite(shop.critical)+' · 预警 '+finite(shop.warning):(stale(shop.latestInventoryAt)?'数据待刷新':'当前正常'))+'</span></button>').join('');q('#mobileShop').innerHTML=all.map(shop=>'<option value="'+attr(shop.id)+'"'+(shop.id===selectedShopId?' selected':'')+'>'+esc(shop.name)+'</option>').join('');}
function renderHero(){const state=recoveryHeadline(overview);q('#hero').className='hero '+state.tone;q('#hero').innerHTML='<h1>'+esc(state.title)+'</h1><p>'+esc(state.detail)+' · 数据汇总于 '+relative(overview.generatedAt)+'</p>';q('#connection').className='pill '+(state.tone==='critical'?'critical':state.tone==='warning'?'warning':'normal');q('#connection').textContent='连接正常';}
function renderStats(){const open=(overview.incidents||[]).filter(item=>item.state==='open');const critical=open.filter(item=>item.severity==='critical').length;const warning=open.filter(item=>item.severity==='warning').length;const unknown=(overview.shopStatuses||[]).reduce((sum,item)=>sum+finite(item.unknown),0);const counts=overview.counts||{};const stats=[['严重风险',critical,'需要立即处理'],['预警风险',warning,'需要尽快处理'],['数据待确认',unknown,'不作为确定性风险'],['商品 / SKU',finite(counts.products)+' / '+finite(counts.skus),'已有库存数据'],['两小时内有效商品',finite(counts.freshProducts)+' / '+finite(counts.products),'超过两小时视为待刷新'],['自动巡检',({running:'正在运行',succeeded:'最近完成',degraded:'部分完成',auth_required:'等待登录',interrupted:'已中断'})[overview.recovery?.state]||'尚无状态','更新于 '+relative(overview.recovery?.updatedAt)]];q('#stats').innerHTML=stats.map(item=>'<article class="stat"><span>'+esc(item[0])+'</span><strong>'+esc(item[1])+'</strong><small>'+esc(item[2])+'</small></article>').join('');}
function reasonText(finding,severity){const source=String(finding?.reason||'').toLowerCase();if(source.includes('2 小时')||source.includes('2小时')||source.includes('2h'))return '预计 2 小时内可能售罄，需要立即处理';if(source.includes('6 小时')||source.includes('6小时')||source.includes('6h'))return '预计 6 小时内可能售罄，需要尽快处理';if(source.includes('24 小时')||source.includes('24小时')||source.includes('24h'))return '可调拨库存不足以支撑未来 24 小时';if(source.includes('未占用')||source.includes('reserve'))return '可调拨库存不足，可能无法覆盖渠道需求';if(/过期|不完整|映射|identity|stale|confidence/.test(source))return '当前数据不足，系统暂停确定性风险判断';return severity==='critical'?'库存与高需求参考存在明显缺口，需要立即核查':'库存与高需求参考接近预警线，需要尽快核查';}
function idsForIncident(incident){const values=[['商品ID',incident.product_id]];for(const finding of incident.findings||[]){const scope=finding.scope||{};values.push(['SKU ID',scope.platformSkuId||scope.platform_sku_id],['商家编码',scope.merchantCode||scope.merchant_code],['渠道品ID',scope.channelGoodsId||scope.channel_goods_id]);}const seen=new Set();return values.filter(([,value])=>{if(!value)return false;const key=String(value);if(seen.has(key))return false;seen.add(key);return true;});}
function copyButton(label,value){return '<button class="copy-button" type="button" data-copy="'+attr(value)+'" aria-label="复制'+attr(label)+'">'+esc(label)+' '+esc(value)+'</button>';}
function filteredIncidents(){return (overview.incidents||[]).filter(item=>item.state!=='resolved'&&(item.severity==='critical'||item.severity==='warning')).sort((a,b)=>(a.severity==='critical'?0:1)-(b.severity==='critical'?0:1)||String(b.last_seen_at).localeCompare(String(a.last_seen_at)));}
function renderIncidents(){const all=filteredIncidents();const pages=Math.max(1,Math.ceil(all.length/INCIDENT_PAGE_SIZE));incidentPage=Math.min(incidentPage,pages);const page=all.slice((incidentPage-1)*INCIDENT_PAGE_SIZE,incidentPage*INCIDENT_PAGE_SIZE);q('#incidentCount').textContent=String(all.length);q('#incidents').innerHTML=page.length?'<div class="incident-list">'+page.map(item=>'<article class="incident-card '+attr(item.severity)+'"><div class="incident-title"><span class="badge '+attr(item.severity)+'">'+severityLabel(item.severity)+'</span><h3>'+esc(item.product_title||item.product_id)+'</h3><span class="badge neutral">'+incidentState(item.state)+'</span></div><p>'+esc(item.shop_name||overview.selectedShop?.name||item.shop_id||overview.shopId)+' · 最近更新 '+relative(item.last_seen_at)+'</p><p>'+esc(reasonText((item.findings||[])[0],item.severity))+'</p><div class="identifier-list">'+idsForIncident(item).map(pair=>copyButton(pair[0],pair[1])).join('')+'</div><div class="incident-actions"><button class="text-button" type="button" data-review="'+attr(item.incident_id)+'">提交运营判断</button></div></article>').join('')+'</div>':'<div class="empty">当前范围内没有正式严重风险或预警。</div>';renderPager('#incidentPager',incidentPage,pages,'incident');}
function renderReminders(){const reminders=Array.isArray(overview.reminders)?overview.reminders:[];q('#reminderCount').textContent=String(reminders.length);q('#reminders').innerHTML=reminders.length?'<div class="reminder-list">'+reminders.map(item=>'<article class="reminder-card '+attr(item.severity)+'"><h3>'+esc(item.title||'运营提醒')+'</h3><p>'+esc(item.detail||'当前状态需要关注。')+'</p><p><strong>建议：</strong>'+esc(item.action||'请核对相关数据后处理。')+'</p></article>').join('')+'</div>':'<div class="empty">当前没有待处理的运营提醒。</div>';}
function productKey(product){return String(product.shop_id||overview.selectedShop?.id||overview.shopId)+':'+String(product.product_id);}
function severityByProduct(){const map=new Map();for(const item of overview.incidents||[]){if(item.state!=='open')continue;const key=String(item.shop_id||overview.selectedShop?.id||overview.shopId)+':'+String(item.product_id);const current=map.get(key);if(!current||item.severity==='critical')map.set(key,item.severity);}return map;}
function productSeverity(product,riskMap){if(stale(product.observed_at)||product.mapping_confidence==='unknown'||finite(product.completeness)<.8)return 'unknown';return riskMap.get(productKey(product))||'normal';}
function searchText(product){return [product.shop_name,product.shop_id,product.product_title,product.product_id,...(product.skus||[]).flatMap(sku=>[sku.platform_sku_id,sku.merchant_code,...(sku.channels||[]).map(channel=>channel.channelGoodsId)])].filter(Boolean).join(' ').toLowerCase();}
function filteredProducts(){const term=q('#search').value.trim().toLowerCase();const filter=q('#riskFilter').value;const riskMap=severityByProduct();return (overview.products||[]).filter(product=>(!term||searchText(product).includes(term))&&(filter==='all'||productSeverity(product,riskMap)===filter));}
function renderProducts(){const all=filteredProducts();const pages=Math.max(1,Math.ceil(all.length/PRODUCT_PAGE_SIZE));productPage=Math.min(productPage,pages);const page=all.slice((productPage-1)*PRODUCT_PAGE_SIZE,productPage*PRODUCT_PAGE_SIZE);const riskMap=severityByProduct();productByKey.clear();for(const product of overview.products||[])productByKey.set(productKey(product),product);q('#productMeta').textContent='显示 '+page.length+' / '+all.length+' 个匹配商品 · 库存最后更新 '+dt(overview.freshness?.latestInventoryAt);q('#products').innerHTML=page.length?'<div class="table-wrap"><table class="inventory-table"><thead><tr><th>店铺</th><th>商品</th><th>风险状态</th><th>SKU 数</th><th>总库存</th><th>快照时间</th><th>数据状态</th><th></th></tr></thead><tbody>'+page.map(product=>{const severity=productSeverity(product,riskMap);return '<tr><td>'+esc(product.shop_name||overview.selectedShop?.name||product.shop_id||overview.shopId)+'</td><td><span class="product-title">'+esc(product.product_title||product.product_id)+'</span><div>'+copyButton('商品ID',product.product_id)+'</div></td><td><span class="badge '+severity+'">'+severityLabel(severity)+'</span></td><td>'+finite(product.sku_count||(product.skus||[]).length)+'</td><td>'+finite(product.total_stock)+'</td><td>'+relative(product.observed_at)+'</td><td>'+(stale(product.observed_at)?'<span class="badge warning">数据待刷新</span>':'<span class="badge normal">数据有效</span>')+'</td><td><button class="text-button" type="button" data-product="'+attr(productKey(product))+'">查看详情</button></td></tr>';}).join('')+'</tbody></table></div>':'<div class="empty">没有匹配的商品，请调整搜索或筛选条件。</div>';renderPager('#productPager',productPage,pages,'product');}
function renderPager(selector,page,pages,kind){q(selector).innerHTML=pages<=1?'':'<button class="page-button" type="button" data-page-kind="'+kind+'" data-page="'+(page-1)+'"'+(page<=1?' disabled':'')+'>上一页</button><span>第 '+page+' / '+pages+' 页</span><button class="page-button" type="button" data-page-kind="'+kind+'" data-page="'+(page+1)+'"'+(page>=pages?' disabled':'')+'>下一页</button>';}
function horizon(forecast,hours){return (forecast?.horizons||[]).find(item=>Number(item.hours)===hours);}
function demandLine(label,value){return '<p><strong>'+esc(label)+'</strong> '+(value==null?'尚未形成':esc(value))+'</p>';}
function showProduct(key){const product=productByKey.get(key);if(!product)return;const detail=q('#productDetail');detail.innerHTML='<div class="drawer-body"><div class="dialog-head"><div><h2>'+esc(product.product_title||product.product_id)+'</h2><p class="muted">'+esc(product.shop_name||overview.selectedShop?.name||product.shop_id||overview.shopId)+'</p></div><button class="icon-button" type="button" data-close-product aria-label="关闭详情">×</button></div><div class="identifier-list">'+copyButton('商品ID',product.product_id)+'</div><div class="detail-summary"><div><span>总库存</span><strong>'+finite(product.total_stock)+'</strong></div><div><span>SKU 数</span><strong>'+finite(product.sku_count||(product.skus||[]).length)+'</strong></div><div><span>数据状态</span><strong>'+(stale(product.observed_at)?'待刷新':'有效')+'</strong></div></div><h3>SKU 与渠道库存</h3><div class="sku-list">'+(product.skus||[]).map(sku=>{const forecast=sku.forecast;return '<article class="sku-card"><h3>'+esc(sku.merchant_code||sku.platform_sku_id)+'</h3><div class="identifier-list">'+copyButton('SKU ID',sku.platform_sku_id)+copyButton('商家编码',sku.merchant_code)+'</div><div class="sku-grid"><div><span>当前库存</span>'+finite(sku.current_stock)+'</div><div><span>已占用</span>'+finite(sku.occupied_stock)+'</div><div><span>未占用</span>'+finite(sku.unoccupied_stock)+'</div></div><div class="identifier-list">'+(sku.channels||[]).map(channel=>copyButton('渠道品ID',channel.channelGoodsId)+'<span class="badge neutral">库存 '+finite(channel.stock)+'</span>').join('')+'</div><div class="forecast-box">'+(forecast?demandLine('常态日需求参考',forecast.daily_p50)+demandLine('高需求日需求参考',forecast.daily_p90)+demandLine('未来 2 小时高需求参考',horizon(forecast,2)?.p90)+demandLine('未来 6 小时高需求参考',horizon(forecast,6)?.p90)+demandLine('未来 24 小时高需求参考',horizon(forecast,24)?.p90):'<p>预测数据尚未形成，不能据此判断库存安全。</p>')+'</div></article>';}).join('')+'</div></div>';q('#productDialog').showModal();}
function renderProductionCycle(){const cycle=overview.productionCycle;if(!cycle||cycle.state==='unavailable'){q('#productionCycle').innerHTML='<div class="empty">BPA Core 暂不可读。库存事实仍可查看，但不能据此判断正式周期正常。</div>';return;}if(cycle.state==='not-run'){q('#productionCycle').innerHTML='<div class="empty">尚无正式库存周期。</div>';return;}if(cycle.state==='in-progress'){q('#productionCycle').innerHTML='<div class="cycle-grid"><div class="cycle-cell"><span>当前状态</span><strong>执行中</strong></div><div class="cycle-cell"><span>计划时间</span><strong>'+dt(cycle.scheduledAt)+'</strong></div><div class="cycle-cell"><span>说明</span><strong>本轮未终态，不回显上一轮健康结论</strong></div></div>';return;}const coverage=cycle.coverage,inventory=cycle.inventory,risk=cycle.risk;if(!coverage||!inventory||!risk){q('#productionCycle').innerHTML='<div class="empty">本轮没有形成可信的全店聚合，不能按 0 家店展示。</div>';return;}q('#productionCycle').innerHTML='<div class="cycle-grid"><div class="cycle-cell"><span>店铺覆盖</span><strong>'+finite(coverage.succeededShops)+' / '+finite(coverage.expectedShops)+'</strong></div><div class="cycle-cell"><span>库存写入</span><strong>'+finite(inventory.persistedProducts)+' / '+finite(inventory.attemptedProducts)+'</strong></div><div class="cycle-cell"><span>风险覆盖</span><strong>'+finite(risk.succeededProducts)+' / '+finite(risk.attemptedProducts)+'</strong></div></div>';}
function render(){renderShops();renderHero();renderStats();renderIncidents();renderReminders();renderProducts();renderProductionCycle();notifyNewItems();}
function notifyNewItems(){const items=[...(overview.incidents||[]).filter(item=>item.state==='open'&&(item.severity==='critical'||item.severity==='warning')).map(item=>({id:'incident:'+item.incident_id,severity:item.severity,title:severityLabel(item.severity)+'：'+(item.product_title||item.product_id),body:reasonText((item.findings||[])[0],item.severity)})),...(overview.reminders||[]).filter(item=>item.notificationEligible!==false&&(item.severity==='critical'||item.severity==='warning')).map(item=>({id:'reminder:'+item.id,severity:item.severity,title:item.title,body:item.detail}))];const next=new Set(items.map(item=>item.id));if('Notification' in window&&seenNotifications&&Notification.permission==='granted'){for(const item of items)if(!seenNotifications.has(item.id))new Notification(item.title,{body:item.body,tag:item.id});}seenNotifications=next;}
async function load(options={}){q('#connection').className='pill neutral';q('#connection').textContent='正在同步';try{if(!csrf)await establishSession();overview=await api('/api/overview?shopId='+encodeURIComponent(selectedShopId));render();q('#connection').className='pill normal';q('#connection').textContent='连接正常';if(options.manual)showToast('已刷新最新数据');}catch(error){q('#connection').className='pill critical';q('#connection').textContent=error.code==='NETWORK_ERROR'?'网络中断':'数据读取失败';showToast(error.message,'critical');throw error;}}
function openReview(incidentId){const incident=(overview.incidents||[]).find(item=>item.incident_id===incidentId);if(!incident)return;q('#incidentId').value=incidentId;q('#reviewProduct').textContent=(incident.shop_name||overview.selectedShop?.name||incident.shop_id||overview.shopId)+' · '+(incident.product_title||incident.product_id);q('#note').value='';q('#reviewDialog').showModal();}
async function submitReview(event){event.preventDefault();const button=q('#submitReview');button.disabled=true;try{const decision=q('input[name="decision"]:checked').value;await api('/api/reviews',{method:'POST',headers:{'content-type':'application/json','x-csrf-token':csrf},body:JSON.stringify({incidentId:q('#incidentId').value,decision,note:q('#note').value})});q('#reviewDialog').close();showToast('运营判断已保存');await load();}catch(error){showToast(error.message,'critical');}finally{button.disabled=false;}}
async function copyValue(value){try{await navigator.clipboard.writeText(value);}catch{const area=document.createElement('textarea');area.value=value;document.body.append(area);area.select();document.execCommand('copy');area.remove();}showToast('已复制 '+value);}
document.addEventListener('click',event=>{const target=event.target instanceof Element?event.target:null;const shop=target?.closest('[data-shop]');const copy=target?.closest('[data-copy]');const product=target?.closest('[data-product]');const review=target?.closest('[data-review]');const page=target?.closest('[data-page-kind]');const closeReview=target?.closest('[data-close-review]');if(closeReview){q('#reviewDialog').close();}else if(shop){selectedShopId=shop.dataset.shop;productPage=1;incidentPage=1;updateUrl();load().catch(()=>{});}else if(copy){copyValue(copy.dataset.copy).catch(()=>showToast('复制失败','critical'));}else if(product){showProduct(product.dataset.product);}else if(review){openReview(review.dataset.review);}else if(page&&!page.disabled){if(page.dataset.pageKind==='product')productPage=Number(page.dataset.page);else incidentPage=Number(page.dataset.page);page.dataset.pageKind==='product'?renderProducts():renderIncidents();}else if(target?.closest('[data-close-product]'))q('#productDialog').close();});
q('#search').value=new URL(location.href).searchParams.get('q')||'';
q('#search').addEventListener('input',()=>{productPage=1;updateUrl();renderProducts();});
q('#riskFilter').addEventListener('change',()=>{productPage=1;renderProducts();});
q('#mobileShop').addEventListener('change',event=>{selectedShopId=event.target.value;productPage=1;incidentPage=1;updateUrl();load().catch(()=>{});});
q('#reload').addEventListener('click',()=>load({manual:true}).catch(()=>{}));
q('#notify').addEventListener('click',async()=>{if(!('Notification' in window)){showToast('当前浏览器不支持桌面提醒');return;}const permission=await Notification.requestPermission();showToast(permission==='granted'?'桌面提醒已开启':'桌面提醒未开启');});
q('#reviewForm').addEventListener('submit',event=>submitReview(event));
establishSession().then(()=>load()).catch(error=>{q('#connection').className='pill critical';q('#connection').textContent='连接失败';showToast(error.message,'critical');});
setInterval(()=>{if(document.visibilityState==='visible')load().catch(()=>{});},60000);
setInterval(()=>{if(document.visibilityState==='visible')establishSession().catch(()=>{});},300000);`;

export const DASHBOARD_TECHNICAL_JS = String.raw`'use strict';
const q=selector=>document.querySelector(selector);
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const finite=value=>Number.isFinite(Number(value))?Number(value):0;
const metric=value=>value==null?'—':Number(value).toFixed(3);
async function api(path){const response=await fetch(path,{credentials:'same-origin'});const body=await response.json();if(!response.ok)throw new Error(body.error||'数据读取失败');return body;}
function chart(backtest){if(!backtest||backtest.status!=='ready'||!Array.isArray(backtest.points)||!backtest.points.length)return '<div class="empty">回测数据不足。</div>';const points=backtest.points.slice(-35),width=1000,height=300,pad=36,max=Math.max(1,...points.flatMap(point=>[finite(point.actual),finite(point.p90)]));const xy=(index,value)=>[pad+index*(width-pad*2)/Math.max(1,points.length-1),height-pad-(finite(value)/max)*(height-pad*2)];const path=key=>points.map((point,index)=>{const position=xy(index,point[key]);return (index?'L':'M')+position[0].toFixed(1)+' '+position[1].toFixed(1);}).join(' ');return '<svg class="chart" viewBox="0 0 '+width+' '+height+'" role="img" aria-label="实际销量与预测回测曲线"><line x1="'+pad+'" y1="'+(height-pad)+'" x2="'+(width-pad)+'" y2="'+(height-pad)+'"/><path class="p90" fill="none" stroke-width="2" d="'+path('p90')+'"/><path class="p50" fill="none" stroke-width="2" d="'+path('p50')+'"/><path class="actual" fill="none" stroke-width="3" d="'+path('actual')+'"/></svg>';}
async function load(){q('#connection').className='pill neutral';q('#connection').textContent='正在同步';await api('/api/session');const data=await api('/api/overview?shopId=all');const backtest=data.backtest||{},metrics=backtest.metrics||{};q('#technicalMetrics').innerHTML=[['P90 覆盖率',metric(metrics.p90Coverage)],['P50 Pinball Loss',metric(metrics.p50PinballLoss)],['P90 Pinball Loss',metric(metrics.p90PinballLoss)],['WAPE',metric(metrics.wape)],['回测窗口',finite(backtest.windowDays)+' 天'],['模型',backtest.model||'—']].map(item=>'<article class="stat"><span>'+esc(item[0])+'</span><strong>'+esc(item[1])+'</strong></article>').join('');q('#backtest').innerHTML=chart(backtest);const cold=data.coldStart||{};q('#coldStart').innerHTML='<div class="cycle-grid"><div class="cycle-cell"><span>直接模型</span><strong>'+finite(cold.directModel)+'</strong></div><div class="cycle-cell"><span>分层回退</span><strong>'+finite(cold.hierarchicalFallback)+'</strong></div><div class="cycle-cell"><span>店铺基线</span><strong>'+finite(cold.storeBaseline)+'</strong></div></div>';q('#cycleTechnical').textContent=JSON.stringify(data.productionCycle||{state:'unavailable'},null,2);q('#connection').className='pill normal';q('#connection').textContent='连接正常';}
q('#reload').addEventListener('click',()=>load().catch(error=>{q('#connection').className='pill critical';q('#connection').textContent=error.message;}));load().catch(error=>{q('#connection').className='pill critical';q('#connection').textContent=error.message;});setInterval(()=>{if(document.visibilityState==='visible')load().catch(()=>{});},60000);`;
